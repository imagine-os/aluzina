/// <reference types="vite/client" />

/** Injected by vite.config.ts from the root package.json version. */
declare const __APP_VERSION__: string;

/** Injected by vite.config.ts: per-build id (base-36 timestamp) used to cache-bust deploy-time thumbnails (D-011). */
declare const __BUILD_ID__: string;
