# Draft for changelog 0013: foundation (Fable 5.1)

Merged into `docs/changelog/0013-*.md` by the integrator with the seven module drafts.

- prompt: 0006 (Justin Massion, Slack #all-aluzina 2026-09-21 04:03 UTC, the Service Delivery Playbook PDF)
- decisions: D-033 (playbook is the canonical service model; 15-status pipeline), D-034 (entities and owners), D-035 (public P-xx inside the OS, P-00 stays linked, payment Placeholder), D-036 (actions bus = WebMCP seam), D-037 (plan.json)
- files: `docs/prompts/0009-service-playbook-and-hub-buildout.md`, `docs/source/playbook/ALUZINA_Operating_System_Services_EN.pdf`, `docs/knowledge/service-playbook.md` (+ README, deliverables, roles-and-portals), `docs/plan/plan.json`, `docs/decisions.md`, `docs/kanban.md`, `docs/build-plan.md`, `docs/README.md`, `docs/reference/surfaces.md`, `apps/hub/src/domain/{playbook,index}.ts`, `apps/hub/src/data/schema/{services,index,projects}.ts`, `apps/hub/src/data/seed/{services,index,projects}.ts`, `apps/hub/src/work/sample.ts`, `apps/hub/src/auth/permissions.ts`, `apps/hub/src/app/{navGroups,manifest}.ts`, `apps/hub/src/i18n/core.ts`, `apps/hub/src/components/atom/StatusPill/StatusPill.tsx`, `apps/hub/src/actions/{bus,useRegisterAction,index}.ts`, `apps/hub/src/plan/index.ts`, `apps/hub/vite.config.ts`, `apps/hub/tsconfig.json`, `apps/hub/src/modules/hub/{HubPage.tsx,specs.ts,strings.ts}`, `apps/hub/src/modules/README.md`
- codes: HUB-01 (cards for P-01, C-01, M-01, D-05..D-11 route-derived)
- rejected: a `phase` -> `pipelineStatus` migration of the Work views in this pass (kept both, D-033); a separate `leadStatus` enum (the four lead statuses are the head of the one pipeline); React in the domain module (pure data, `pick(text, lang)` for rendering)
- verification: `npm run build` green (tokens + tsc + vite + copy-static); Vite SSR check of the seeds (36 entities, SEED_VERSION 5, every `engagements.checks` key resolves to a real phase item, every playbook status has a `core.status.*` string and a `StatusPill` tone, `plan.json` imports and orders 59 tasks). No screenshots in this pass.
- deferred (integrator): `scripts/thumbnails.mjs` targets for the new codes; `docs/pages/HUB-01.md` layout update; version bump to 0.8.0; Spanish review of `domain/playbook.ts` by the founder; `core.status.concept / briefing / approved / delivered` are shared with older vocabularies (same meaning).
