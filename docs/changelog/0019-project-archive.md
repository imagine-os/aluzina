version: 0.11.0
date: 2026-09-21
prompt: 0017
intent: Bring Aluzina's Dropbox project archive (2019-2026, four share links) into the OS as data: an intake pipeline (crawl -> redacted index -> renders -> seed), a project archive browser that separates prospects / in progress / past with a "see everything" default and a portfolio set, an example portal view of the featured project JOE GALLINA INTERIOR with every deliverable previewable inside the system, icons / thumbnails / previews as library components, tags, and the work split into tasks ar-01..ar-18.
decision: D-055 (files as `assets.kind 'file'` + archive fields, projects tags / cover / year / source folder, JSON in docs/ as the seed source), D-056 (lifecycle derived from `pipelineStatus`, three shelves + All, no `archived` flag), D-057 (14 delivery stages as the document order, deepest folder wins, the studio's numbering is first-class), D-058 (share links crawled with headless Chromium; served renders are the visual memory, nothing under docs/; partially supersedes D-049 for archive files), D-059 (privacy: the public build carries no identifiable personal or financial document; redaction rules, previews only for design deliverables, lifted when auth + file storage exist), D-060 (inferred facts are marked as inferred), D-061 (portfolio set in localStorage until Spaces collections; PDF export waits for R3)
rejected: Dropbox API / OAuth (no credentials exist and the share links suffice for reading; the connector is task ar-13 behind the R9 seam); storing renders under `docs/` next to the served copies (double bytes for 4.3 GB folders); an `archived` boolean on `projects` (lifecycle is derived from the status the CRM already keeps); previewing administrative / financial files (privacy, D-059); guessing client names from folder names (only existing client rows match, D-060); keeping the original share link on a redacted file (Dropbox links embed the file path, so the link is replaced by the folder's, R6)
files: scripts/archive/crawl-dropbox.mjs, scripts/archive/build-index.mjs, scripts/archive/render-previews.py, package.json (0.11.0, `archive:*` scripts), apps/hub/package.json (0.11.0), docs/archive/{README.md,index.json,projects/joe-gallina-interior/index.json}, apps/hub/public/archive/joe-gallina-interior/{thumbs,pages}/ (111 JPEGs, 6.1 MB), apps/hub/src/domain/archive.ts, apps/hub/src/domain/index.ts, apps/hub/src/data/schema/{assets,projects}.ts, apps/hub/src/data/seed/{archive,assets,projects,index}.ts, apps/hub/src/work/sample.ts, apps/hub/src/modules/founder/LeadsPage.tsx, apps/hub/src/components/atom/FileIcon/*, apps/hub/src/components/molecule/Thumb/*, apps/hub/src/components/organism/DocumentViewer/*, apps/hub/src/modules/archive/{index,specs,strings,model}.ts + {ArchiveBrowserPage,ProjectPortalPage}.tsx + archive.css, apps/hub/src/modules/hub/{specs,strings}.ts + HubPage.tsx, scripts/thumbnails.mjs, docs/pages/{S-12,S-13,HUB-01}.md, docs/screenshots/{S-12,S-13}/* + HUB-01/en-1280.jpg, docs/qa/0003-archive-smoke.md, docs/knowledge/{archive,clients}.md, docs/prompts/0017-past-projects-dropbox-archive.md, docs/decisions.md, docs/build-plan.md, docs/plan/plan.json, docs/kanban.md, docs/reference/surfaces.md, docs/README.md, apps/hub/src/modules/README.md
codes: S-12, S-13, HUB-01, D-02
model: Fable 5.1 (architecture, shared code, integration, privacy redaction, docs), Opus 5 (S-12 / S-13 module), Sonnet 5 (Dropbox crawls, inventory, featured-project renders)

# 0019 - Project archive: Dropbox intake pipeline, S-12 browser, S-13 project portal, FileIcon / Thumb / DocumentViewer

## A. What Justin asked and what shipped

Justin (Slack #past-projects, prompt 0017) shared four Dropbox folders: the featured, "very well organized" JOE GALLINA INTERIOR; all 2026 projects; "all 2019-2023" (a share that also holds the 2020, 2021, 2022, 2024 and 2025 year folders); and "00 INFORMACION RELEVANTE ALUZINA 2023". He asked for a pipeline that organises the documents in order, an example portal view of the featured project with every item previewable inside the system, tags, icons / thumbnails / previews across the system, a clean separation of past vs in-progress vs prospects with a "see everything" option, portfolio and client example sets, and the work split into tasks.

Shipped in this pass:

- **Intake pipeline** (section B): `scripts/archive/crawl-dropbox.mjs`, `build-index.mjs` (with the D-059 redaction and a self-check), `render-previews.py`; `npm run archive:crawl / archive:index / archive:previews` document the invocations.
- **Data**: `docs/archive/index.json` (187 project folders, one entry each with direct children) and `docs/archive/projects/joe-gallina-interior/index.json` (179 files, redacted); 25 thumbnails + 86 page renders served from `apps/hub/public/archive/joe-gallina-interior/` (6.1 MB).
- **Model and seed** (sections C, D): `assets.kind 'file'` with `source / sourceUrl / folderPath / thumbnailUrl / previewUrls / stage / year`, `projects.tags / coverAssetId / year / sourceFolderUrl`, `domain/archive.ts`, `seed/archive.ts` -> 186 archived projects (2 prospects, 18 in progress, 166 past), 1392 file assets (341 `confidencial`), 195 Spaces (7 year areas + Administrativo + one space per project), 1 folder-tree note, 1626 relations, 42 tags.
- **Library** (section E): `FileIcon` atom, `Thumb` molecule, `DocumentViewer` organism (with `controls` and the image `onError` fallback), all on `/#/dev/components`.
- **Pages** (section F): S-12 project archive browser and S-13 project portal on studio, founder and brand; hub card S-12 in Product surfaces.
- **Docs**: this changelog, decisions D-055..D-061, build-plan step 14, plan.json tasks ar-01..ar-18, kanban, `knowledge/archive.md`, `knowledge/clients.md` row, page docs, screenshots, `docs/qa/0003-archive-smoke.md`, surfaces.md, both READMEs.

Repository visibility check (A6): `GET https://api.github.com/repos/imagine-os/aluzina` -> `"private": false, "visibility": "public"`. The hub therefore deploys to a **public** GitHub Pages site, which is why section G applies to every committed byte.

## B. Intake pipeline (crawl -> index + redaction -> renders -> seed)

1. **Crawl** (Sonnet 5, promoted to `scripts/archive/crawl-dropbox.mjs`). Headless Chromium opens the Dropbox share page (`dl=0` viewer), scrolls until the list stops growing, reads the file table or card grid, detects the sign-in wall and the 429 page, paces >= 1.5 s between pages and retries once after 30 s; read-only, TLS verification never disabled (the proxy CA is trusted by the environment, not by a flag). `--url --depth --out` walks one folder BFS; `--targets` lists one page per project folder and is resumable. **The featured link (A) asks for a Dropbox sign-in when opened directly**; the same folder is public inside link C (`PROYECTOS ALUZINA 2019 2023/JOE GALLINA INTERIOR`) and was read from there. Link D was listed (12 files: price lists, catalogues, two brochures, presentations) and is intake task ar-15.
2. **Inventory** (depth 1 per project folder). The first pass covered 117 folders because the root listings had been paginated at 16 entries per year folder; the integrator's completion crawl (77 more targets, 60 s pause, 2 s pacing, zero rate limits) brought the total to **187 project folders**: 2026 18, 2025 37, 2024 43, 2022 25, 2021 23, 2020 25, 2019-2023 14 (incl. the featured project), root 2 (LIFE VIOLETA VILLA, whose eight root-level subfolders were consolidated into one project, and SANTIAGO AGUIRRE ILUMINACION, which also appears as `05 SANTIAGO AGUIRRE ILUMINACION` in 2025). **1225 files and 243 subfolders** at the first level, about **20 GB** of listed sizes (Dropbox display values). Kinds: 184 project, 2 quotation-only folders (prospects), 1 admin folder (`FACTURAS DE VENTA Y CUENTAS DE COBRO 2022`). 15 folders are empty; **0 unlisted**: `PROYECTOS 2026/02_LINA ZAPATA` (rate-limited in the first pass) listed 26 files on the retry.
3. **Featured deep crawl** (Sonnet 5): JOE GALLINA INTERIOR, **179 files in 104 folders, 4.3 GB**; 28 design files downloaded within a 400 MB budget (the 0.3-1 GB presentation PDFs streamed one at a time, rendered, deleted), rendered with PyMuPDF / Pillow / LibreOffice: 30 thumbnails, 20 documents with page renders (first 12 pages at 1400 px). Administrative, supplier and closing folders were never downloaded (privacy skip-list in the worker's `privacy.py`, now rule R2).
4. **Index + redaction** (`build-index.mjs`, section G): 200 of 1225 inventory files and 147 of 179 deep-index files redacted, 12 folder segments rewritten, 347 share links replaced by a folder link; self-check passed. Output shapes are `Inventory` / `DeepIndex` in `seed/archive.ts`; the stubs are gone.
5. **Served renders** (`render-previews.py serve`): the renders the redacted index still references, re-encoded to thumbs <= 640 px q80 and pages <= 1200 px q72, at most 8 pages per document: **25 thumbnails, 86 pages, 6,103,950 bytes** under `apps/hub/public/archive/joe-gallina-interior/{thumbs,pages}/`; every referenced file exists (the serve step prunes otherwise). Nothing under `docs/` (D-058); `repoPath` points at the served file.
6. **Seed** (`seed/archive.ts`, order 70): derives everything from the two JSON files; `npm run build` green.

## C. Data model (D-055) and seed rules

- `Asset` += `kind: 'file'`, `source`, `sourceUrl`, `folderPath` (verbatim studio folder names, rewritten only where a segment named a person), `thumbnailUrl`, `previewUrls[]`, `stage`, `year`; `Project` += `tags`, `coverAssetId`, `year`, `sourceFolderUrl`; `SEED_VERSION` 7. Brand pages and portfolio projects carry the new fields (`source: 'repo'`, `stage: 'marketing'`, `tags: ['portfolio']`); the six live projects have empty tags.
- One `projects` row per inventory entry of kind `project` **or `quote`**, id `prj-ar-<slug>`: type from the folder name, status from the year folder (2026 -> `contracted` / phase `development`; quote -> `proposal-sent` / phase `lead` / tag `cotización`; else `closed` / `delivered`), client `'unknown'` unless the name says SODIME / COASSIST / HOY / SPORTI (relation `for-client`; 6 matches, 180 unknown), `startDate` `<year>-01-01` (2019 for the 2019-2023 range and LIFE VIOLETA VILLA), summary in Spanish saying what is inferred plus the `note` from the index (duplicates, empty, inferred year). `kind: 'admin'` folders are not projects: their files are `posts` of kind `file` in the area `sp-ar-admin`.
- One `assets` row per child file (shallow) or per deep-index file (featured), id `ast-ar-<project>-<slug>[-n]`, `tags: ['archive', <fileType>, <stage>]` + `confidencial` when redacted; redacted rows keep the redacted title, no excerpt, no thumbnail, no pages. Cover = the first asset with a thumbnail that is an image or a PDF (1 project has one in this pass).
- Spaces: 7 year areas + `Administrativo` under `sp-archive`, one project space per archived project (`aboutType: 'projects'`), the featured project's pinned Spanish note with its folder tree by stage; relations `belongs-to` (asset -> project), `depicts` (images and design-stage files of the featured project), `produced-by` (project -> role founder), `for-client`, `references` (08_HONEY VALLEY LUMINARIA <-> the hub's Honey Valley project, to confirm).
- Tags registry: `archive`, `dropbox`, `cotización`, `confidencial`, each year folder, the Spanish type tags and every delivery stage (42 tags in total after the other seeds).

## D. Lifecycle, stages and tags (D-056, D-057)

`lifecycleOf(pipelineStatus)` -> prospect / active / past; S-12 shows all 205 projects (186 archived + 13 portfolio + 6 live) with the three shelves as filters and "All" as default: 3 prospects, 23 in progress, 179 past. `DELIVERY_STAGES` (brief, survey, references, concept, design-development, technical, quotation, procurement, production, execution, delivery, admin, marketing, other) order a project's documents; `stageFor` classifies deepest folder first. Distribution over the 1392 file assets: procurement 114, quotation 98, admin 57, design-development 43, concept 31, technical 31, delivery 10, production 8, survey 4, execution 4, references 1, marketing 1, **other 990** (files at the top level of a project folder have no folder context; the deep indexes of ar-06 / ar-07 fix that). The studio's folder numbering (`00_` .. `011_`, `0_69` .. `0_112`, `01` .. `038`) is kept and sorted numerically (`compareFolderPaths`), shown as a muted prefix in "By folder".

## E. Library components

- `FileIcon` (atom): inline SVG per `FileType` (pdf, image, vector, presentation, spreadsheet, document, cad, model3d, video, audio, archive, folder, other), a distinct inner mark per type (never colour alone), `role="img"` + label, tints as `color-mix` of existing tokens.
- `Thumb` (molecule): image with `FileIcon` fallback on null `src` or load error, ratios 4:3 / 16:9 / 1:1 / 3:4, badge and caption, lazy; a plain `<figure>` the parent makes interactive.
- `DocumentViewer` (organism): paged image viewer over served renders (prev / next 44 px, arrow keys, counter, thumbnail strip), PDF `<object>` / `<iframe>` chain, image, video, or icon + "Open at source" / "Download". Integration adds `controls?: boolean` (S-13 passes `false` because it renders its own action row) and an `onError` fallback for the image mode, so a missing render reads as a state rather than a broken `<img>`. G-08's `DocFrame` migrates in ar-17.

## F. Pages S-12 / S-13 (merged from the module draft, Opus 5)

- module `apps/hub/src/modules/archive/`: routes `/{studio,founder,brand}/archive` (S-12) and `/{studio,founder,brand}/archive/:projectId` (S-13), permission `projects.read`, shell `desktop`, nav `archive.nav.archive` order 8 glyph `▤` in navGroup `projects`; `specs.ts` (`archiveBrowserSpec(surface)`, `archivePortalSpec(surface)`, `checkedAt: [360, 390, 768, 1280, 1920]` verified with Playwright); `strings.ts` 144 keys EN + ES (Colombian Spanish); `model.ts` (`useArchiveData()`, `usePortfolioSet()`, `topFileTypes`, `stageCounts`, `formatBytes`); `archive.css` tokens only, prefix `arc-`.
- **S-12 Project archive browser**: `PageHeader` with Portfolio set toggle and Import folder (Placeholder); four `StatTile` buttons Prospects / In progress / Past / All with file counts; `FilterBar` (search over name, client, tag, year; year, type, tag, sort selects); Cards | Table; cards show the cover `Thumb` (or `FileIcon`), client, year, file count, status pills and tags; the set bar with Copy set, Open in Spaces (Placeholder) and Export PDF (Placeholder). 12 actions: `archive.filterLifecycle`, `filterYear`, `filterType`, `filterTag`, `search`, `setView`, `openProject`, `toggleInSet`, `copySet`, `clearSet`, `exportSet`, `importFolder`.
- **S-13 Project portal**: hero (cover render, client, location, type, status pills, tags, Open in Dropbox / Open in Work / Open space, Add to portfolio set); the **delivery order rail** (every stage with its file count, keyboard ArrowLeft / ArrowRight / Home / End); files grouped **By stage** or **By folder** (the studio's numbering), filters by stage / file type / search, grid or list of `Thumb`s; a `Drawer` with `DocumentViewer` (page renders, prev / next file, position), meta (type, size, date, stage, folder), Download / Open at source / Copy link; related client, note, Work and space cards. 16 actions: `archive.openFile`, `closeFile`, `nextFile`, `prevFile`, `previewPage`, `filterStage`, `filterFileType`, `searchFiles`, `setFilesView`, `setGrouping`, `openSource`, `downloadFile`, `copyFileLink`, `openFolderSource`, `tagFile` (Placeholder, `assets.manage`), `addToSet`.
- All 28 actions are declared and registered while mounted; the Placeholders answer `'not wired yet: …'` (D-047). Verified through `window.__aluzina.actions.run`: `archive.filterLifecycle {life:'past'}` -> `{ ok: true, result: 'past' }`; `archive.exportSet` -> `{ ok: false, error: 'not wired yet: PDF export lands with the documents module R3' }`; `archive.openFile {asset}` -> the file title, drawer open (section I).
- Module-pass verification (Opus 5): tsc + build green; 360 / 390 / 768 / 1280 / 1920 with 0 px overflow, 0 console errors, no target under 44 px; S-12 tab sequence one stop per card; S-13 rail arrow keys with the global focus ring, Enter opens the drawer and moves focus into it, Escape closes; Spanish at 1280 on both pages.
- Integration: hub card `{ id: 'archive', code: 'S-12', key: 'archive', kind: 'route', enterAs: 'studio', enterUnless: 'projects.read' }` in Product surfaces, `hub.cards.archive.*` EN + ES, `SURFACE_IDS` += `archive`; `scripts/thumbnails.mjs` targets S-12 and S-13; the module's requests 1-7 in its draft are all done here (hub card, served renders, viewer fallback, `controls`, surfaces.md, kanban / plan, Next free S-14); request 5 (client `unknown`, `titleCase` names) stays by design (D-060) until ar-08.

## G. Privacy (D-059)

Rules, as the header table of `build-index.mjs` (R1..R7):

- **R1** file name (accent-stripped, lower-cased, `_` as space) matches `rut | seguridad social | seg soc | planilla | autoliquidacion | arus | cedula | tarjeta profesional | contrato | contract | agreement | comprobante | cuenta de cobro | cuentadecobro | factura | fv- | fra | cxc | pedido | cotizaci | quotation | invoice | whatsapp image | pago(s) | payment | cash | asana | .xml | "c.m "` (short tokens as whole words) -> name `"<Tipo> (redactado).<ext>"`, `redacted: true`; ext, size, date kept; no excerpt / thumbnail / pages / palette. Tipo in {RUT, Seguridad social, Documento personal, Contrato, Comprobante de pago, Cuenta de cobro, Factura, Cotización, Pedido, Imagen de WhatsApp, Documento financiero, Documento de cierre}.
- **R2** any folder segment matches `administrativo y financiero | suppliers and financial status | cierre de proyecto | contables | cuentas de cobro | facturas` -> same as R1 (Tipo from the name, else Documento financiero / Documento de cierre); a file named "logo" keeps its name.
- **R3** text excerpt carries personal data (fecha de nacimiento, cédula / C.C., NIT), the name says "feng shui" (the owner's birth chart), or the file is on the reviewed list (the delivery form naming the client and apartment with an internal process review; the two contractor-dispute letters) -> name kept, previews and excerpt dropped, `redactedReason` set.
- **R4** person-named folder segments -> role (`suppliers and financial status/04_ALUZINA/<NN NAME ROLE>` -> `NN EQUIPO <ROLE>`, `10_MERY & SONS/<NN NAME>` -> `NN CONTRATISTA`, `01_AJOTA … ARTISTA` -> `01_ARTISTA`, `02_ALEX … ELECTRICO` -> `02_ELECTRICISTA`, `06_DOMOTICA YAKO DAVID` -> `06_DOMOTICA`); explicit table, 12 segments rewritten.
- **R5** company folder names and project folder names stay (the studio's identifiers; Justin can ask for any to be redacted).
- **R6** (added on the coordinator's review) Dropbox share links embed the full path URL-encoded, so a redacted file's `href` / `sourceHref` and a person-named folder's `href` are replaced by the link of the nearest **safe ancestor**: the company-level supplier folder, or the project root when the parent is a person folder or the file sits under ADMINISTRATIVO Y FINANCIERO / CIERRE DE PROYECTO / CONTABLES / CUENTAS DE COBRO / FACTURAS. 347 links replaced.
- **R7 self-check** before anything is written: no redacted entry's link (URL-decoded, beyond the project root) matches the pattern or a replaced person segment; no `name` / `path` / excerpt anywhere contains a replaced segment or a person's full name (the founder's name is public and exempt); no unredacted file name matches the pattern; the whole output is also grepped for every replaced segment plain and URL-encoded. Any hit exits 1. **Result this run: passed** over 1468 inventory entries (children of 187 folders) and 179 deep-index files; recorded as `privacyCheck` in `docs/archive/index.json`. The first run of the check caught two real gaps (the founder's name in two "conflicted copy" file names and in a pptx excerpt, exempted as public; redacted WhatsApp images under a project folder that itself names a person, accepted as R5) and no leaks after the R6 fix.

Counts: inventory 200 of 1225 files redacted; deep index 142 of 179 by name / folder + 5 by content = 147; previews exist for 25 design files only. Previews of anything under an administrative, supplier or closing folder were never downloaded.

## H. Inferred facts to confirm with the founder (D-060, task ar-08)

1. Project **type** per folder (residencial / comercial / hospitalidad / bienestar / iluminación) — inferred from words in the name; 137 landed on "comercial" as the default.
2. **Status**: are all 18 folders in `PROYECTOS 2026` really in progress (contracted)? Are the 166 older folders closed, or are some still open (e.g. `BROOKLING PIZZA` 2022 with files from Aug 2025, `PAMPAS ROOM STARTER` with files from May 2024)?
3. **Quotation-only folders as prospects**: `COTIZACION HELADERIA` (2022) and `011 COTIZACION LUMINARIA CORAZON` (2025) — were they won, lost or still open?
4. **Clients**: only SODIME, COASSIST, HOY and SPORTI matched existing rows; the other 180 projects have `client: unknown`. Which folder names are client names to be created as `clients` rows, and which are locations or internal work (e.g. `0_76 OFICINA ALUZINA`, `Proteccion Anticontagio ALUZINA`, `026 CURSOS DE INTELIGENCIA ARTIFICIAL`)?
5. **Duplicates / continuations**: SODIME 2020 / 2026; COASSIST 2020 / 2021; EL ENCANTO 2024 / 2025; SANTIAGO AGUIRRE ILUMINACION root / 2025; SIMON CALERA 2024 / 2026; ALMA PRANA 2025 / 2026; UPPER TRIP 2021 / UPPERTRIP + UPPERTRIP TUBO 2024; VILLA VERDE 2021 / SEGUNDO PROCESO 2024; SHABELA 2021 / SHABELA FOTOS + SHAVELA 2022; BONNY 2019-2023 / 2021 / 2025; NATHALY KENEDY 2024 / 2025; CASA JORGE Y LIGIA / LIGIA Y JORGE 2024; EL SILENCIO DE LOS PAJAROS 2024 / 2025; APTO PAOLA 2021 / APARTAMEMTO PAOLA JIMENA 2022; 08_HONEY VALLEY LUMINARIA 2026 / DESARROLLO DE ILUMINACION 2024 (and the hub's Honey Valley project).
6. **Years**: the 2019-2023 folder's 14 projects have no year (files dated 2023-2024); LIFE VIOLETA VILLA inferred 2019 from its oldest file (`set/SODIACO SET.xlsx`, Nov 19 2019). Is JOE GALLINA INTERIOR a 2023 project?
7. **Container folders**: `PROYECTOS ALUZINA 2020 GRAFICOS` and `PROYECTOS 2021 SEGUNDO SEMESTRE` look like groupings, not projects.
8. **Empty folders** (15): work elsewhere, or folders opened ahead of the project?
9. **Featured project**: `PRESENTACION TRONCAL (1).pptx` (a 2024 university presentation) seems misfiled; the two 52-page presentations look like the same document re-exported; is "Altos de la Toja 402" the project's name for the portfolio?
10. **Redaction choices**: project folder names that contain people's names stay as identifiers (e.g. `MARTA OVIEDO BAÑO SOCIAL`, `03_ANDRES Y ANDREA`); company supplier names stay. Anything to redact further, or to show (the delivery form, the feng shui report) once the site is behind sign-in?
11. **Link D** (`00 INFORMACION RELEVANTE ALUZINA 2023`): price lists, catalogues, brochures, presentations — company assets for G-08 (ar-15); confirm which are current.

## I. Verification

- `npm run typecheck` clean; `npm run build` green (tokens + tsc + vite + copy-static; the pre-existing 1.5 MB chunk warning only); `dist/archive/` 6.1 MB.
- Seed against the real data (read from the running app's `aluzina.data`): `SEED_VERSION` 7, 37 tables, 205 projects (186 `prj-ar-*`: prospect 2 / active 18 / past 166; all: 3 / 23 / 179), 1450 assets (1392 files, 341 `confidencial`, 25 with thumbnail, 15 with pages, 86 pages), 195 `sp-ar-*` spaces, 7 posts, 14 filings, 1626 relations, 42 tags; store 2.4 MB of localStorage (watch as deep indexes land).
- Playwright smoke (`docs/qa/0003-archive-smoke.md`): S-12 and S-13 as studio, founder and brand at 390 and 1280 -> 12 / 12 pass, 0 console errors, 0 failed requests, 0 px horizontal overflow; the drawer over `00 PLANOS ALTOS DE LA TOJA.pdf` loads `pages/…-page-01.jpg` (1200 px, HTTP 200); hub card `archive` Live; dark theme applies.
- Screenshots re-captured against the local preview of this build: `docs/screenshots/S-12/{en-390,en-1280,en-1920,es-1280}.jpg`, `docs/screenshots/S-13/{…same}.jpg`, `docs/screenshots/HUB-01/en-1280.jpg`; the 1280 shots were read back (counts, cover, rail, no overflow).
- Privacy self-check R7 passed (section G).

## J. Deferred / next (plan.json step 14)

ar-06 deep index + previews for every 2026 project (Sonnet 5, next); ar-07 the same for 2020-2025 per year folder (backlog); ar-08 founder confirmation of section H, then the seed fix (Fable 5.1 + founder, next); ar-09 tag editing and cover picking in the product (`archive.tagFile`, `archive.setCover`, `assets.manage`; Opus 5, next); ar-10 portfolio sets as Spaces collections + Export PDF via R3 (backlog); ar-11 icon system: SVG set replacing Unicode nav glyphs, entity icons, `Thumb` on hub cards / Spaces / catalog (Fable 5.1 design, Opus 5 build, next); ar-12 file storage seam + Supabase storage + auth gate before any personal document is served (lifts D-059; backlog, after step 11); ar-13 Dropbox API connector behind R9 (backlog); ar-14 client example set page P-06 (backlog); ar-15 intake of link D as company assets on G-08 (Sonnet 5, next); ar-16 re-crawl the 15 empty folders later (next); ar-17 G-08 DocFrame -> DocumentViewer, `Thumb` in K-05 and Spaces file posts (backlog); ar-18 QA at 2560 / 3840 + dark, Spanish review with the founder (next). Also noted: files at a project's top level classify as stage "other" until deep indexes exist; the `--tint-*` colours of `FileIcon.css` could be promoted to `tokens.ts`; the seed's localStorage footprint (2.4 MB) needs a lazy or split store before the 2026 deep indexes land.
