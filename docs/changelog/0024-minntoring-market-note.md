version: 0.15.0
date: 2026-09-22
prompt: 0022
intent: File the Minntoring sales-webinar deck that Aleja Guerra received (54 slides, June 2025, Spanish) as internal market intelligence: one `reference` note under the new `docs/knowledge/market/` with what the deck is, why it matters, a bullet-by-bullet validation checklist of its "3 pillars" against the playbook (14 answered, 11 partial, 6 gap), the three gaps as backlog candidates (niche / value offer, client acquisition beyond word of mouth, automation), the pressure tactics not to copy, and the handling rule; the PDF stays off the repo and the public site.
decision: D-087 (third-party market material is filed as a `reference` note under `docs/knowledge/market/`, never as the document itself; the original stays at source and is never committed or served)
rejected: committing the PDF to `docs/source/` (third-party copyrighted work in a public repo; `docs/source/` holds only Aluzina's own material and D-068 already keeps third-party documents index-only); committing the full text extraction (same reason; the note quotes only the lines that carry the evidence); serving anything under `apps/hub/public/`; giving the note `status: current` or `draft` (it states no Aluzina rule, so a distinct `reference` status was added to the entry convention); a third backlog card for automation (the document generators, alert generation and connector cards already cover it); recording the testimonial individuals by name (studios only) or any contact details (there are none)
files: docs/knowledge/market/minntoring-webinar-2025.md (new), docs/knowledge/README.md (`reference` status in the convention, files row, change log), docs/README.md (knowledge row points at `knowledge/market/`), docs/decisions.md (D-087), docs/kanban.md (two Backlog cards, one Done card), docs/plan/plan.json (`kb-market-positioning`, `kb-market-acquisition`, updatedAt), docs/reference/surfaces.md (change-log line, no surface change), docs/prompts/0022-minntoring-webinar-deck.md (new), docs/changelog/0024-minntoring-market-note.md (this file)
codes: D-06, D-15 (the note renders in the in-app docs viewer like every knowledge file; no UI change)
model: Fable 5.1 (assessment reply, the note and its checklist, D-087; the worker steps of reading the deck, verifying the playbook references and filing the docs also ran on Fable 5.1)

# 0024 - Minntoring webinar deck filed as market reference (docs only)

Justin forwarded to #all-aluzina (prompt 0022) the PDF Aleja Guerra received from Minntoring, an architecture-business coaching company, as the opening material of a roughly USD 10k mentoring programme she took the call for and did not join, asking whether it relates to the work. The assessment reply (22:55 UTC) read it as a sales funnel that doubles as a validation checklist for the Aluzina OS and offered to file it; Justin answered "Yes please" (00:20 UTC). No app code changed.

1. **The note** `docs/knowledge/market/minntoring-webinar-2025.md` (new folder, first entry with `status: reference`). Six sections: (1) what the deck is, slide range by slide range, with the Spanish pillar names "Claridad y Enfoque", "Arquitectura de Procesos", "Escalabilidad y crecimiento", the five common problems, the founders' claims (19 spaces in 2018 to nearly 200 in 2023, 30 simultaneous projects, 60% of founder time freed, 500+ architects from 19 countries, recorded as claims), the three testimonials by studio (RIUX, H Arquitectura, Metier Studio with its "implementar Pipefy"), the myths, the three-word programme format, the "two options" close and the strategy-call button; (2) why it matters: competitive intelligence on how studios like Aluzina are sold to, and a free checklist; (3) **the validation checklist**: every one of the 32 problem / solution bullets of the six pillar slides, verbatim, mapped to the playbook and the product with a status. 14 answered (one method and the 10-step journey, G-01..G-09 and G-10..G-14, the single revision matrix and change orders, the 15 pipeline statuses, the single traceable pipeline and routing, the 11 operational assets, the manual), 11 partial (all rooted in the playbook's own unwritten "next layer": who does each stage, durations, templates, triggers; plus alerting), 6 gap (each of the three gaps named twice by the deck), 1 n/a (mindset); (4) the gaps as candidate backlog items: (a) niche and unique value offer / value not price, (b) a client-acquisition strategy beyond word of mouth (the commercial process starts at lead entry), (c) automation of repetitive tasks (partly what the Hub is; covered by existing cards); (5) what not to copy: the "two options" close, the "surprise if you stay to the end" hook and "if you keep doing the same you get the same results" against the playbook's "Each step must generate enough clarity to justify the next - never pressure" and the commercial rule; (6) handling: third-party copyrighted, PDF not committed, original in the Slack thread, no personal contact details.
2. **Playbook references verified** against `docs/knowledge/service-playbook.md` before citing: the operating logic and journey (p. 2), the commercial process and rule (p. 3), the 19 stages of service 03 with stage 10 validation and the approval gate, the E stage 8 change-control rule, the ladder's business logic, governance rules G-01..G-09 (+ G-10..G-14 as carried in `domain/playbook.ts`), the six roles and their map onto `team.md`, the 11 operational assets, the 15 statuses and the 10 KPIs; plus `roles-and-portals.md`, `deliverables.md`, `asana-conventions.md`, `tools-in-use.md`, `brand.md`, `social-channels.md`. Page codes cited: A-01, A-03, A-08, S-10, S-11, O-11, O-12, O-13, C-03, W-01..W-03, M-01..M-08, P-01..P-03.
3. **Decision D-087** in `docs/decisions.md`: third-party market material becomes a `reference` note, never the document itself. `docs/knowledge/README.md` gains the `reference` status in its entry convention, the files-table row and a change-log line; `docs/README.md`'s knowledge row points at `knowledge/market/`.
4. **Kanban and plan**: two Backlog cards for gaps (a) and (b) (`kb-market-positioning`, depends on the open brand-era question `kb-await-brand-era`; `kb-market-acquisition`, depends on the marketing portal card) mirrored in `plan.json` so the D-05 viewer agrees with `kanban.md` (D-037); one Done card for this filing. No new card for (c).
5. **surfaces.md**: change-log line, no surface change.

## Why the PDF is not in the repo

`docs/source/` holds Aluzina's own material (playbook, brand manuals, marketing PDFs, its own site and social captures, its Asana exports). The one document there that came from outside (the SELAV diagnostic inside the Claude Design export) arrived as part of a deliverable to Aluzina. The Minntoring deck is another company's copyrighted sales material, and this repository is public, so committing it would publish it; D-068 already keeps the third-party brochures and catalogues of the Dropbox archive index-only for the same reason. The note carries the summary and the short quotations the comparison needs; anyone who needs the deck opens it in the Slack thread named in the note's header.

## Verification

Docs-only change; no routes, actions, strings or components touched. `npm run build` green before push (the docs are imported through `@docs`, so a malformed `plan.json` would break the build). Numbers taken after `git fetch origin main`: changelog 0024, prompt 0022, D-087 (no `_pending/` claims at the time).
