version: 0.14.0
date: 2026-09-21
prompt: 0020
intent: Third pass of step 14 (the project archive), Justin's "Continue until complete": the archived files leave the seed for lazy per-project chunks so the store has room (ar-19); deep indexes and served previews for every 2019-2025 and root folder (ar-07); a client-facing example set page reached by a stateless link, with "Save set to Spaces" (ar-14, ar-10); a founder page where the inferred facts of the archive are confirmed in the product (ar-23, replacing the chat-thread ar-08); G-08 and Spaces on the shared DocumentViewer and Thumb (ar-17); the focus-trap fix, the last Unicode glyphs and the HOY stage classification (ar-20, ar-21, ar-22).
decision: D-071 (archived files are lazy chunks + patch rows, never seeded), D-072 (`useFocusTrap` keeps `onClose` in a ref), D-073 (string icon / glyph resolution inside `Button`, `StatTile`, `EmptyState`), D-074 (a shared set is a stateless link), D-075 (confidential never leaves the studio: no preview, no mention), D-076 (Spanish is the default of client-facing pages, which translate themselves), D-077 (`archive.curate` gates "Save set to Spaces"; studio gets no `spaces.write`), D-078 (confirmation = tag + summary edit), D-079 (duplicate = `replaces` relation), D-080 (open questions are page data), D-081 (clients are created from the archived project row), D-082 (render budgets for 2019-2025: ~12 MB per year, pages dropped before thumbs)
rejected: keeping file rows in the seed with a smaller shape (still ~1000 more rows for ar-07, and edits would keep living on seeded rows); a share token or a `sets` table for P-06 (nothing to revoke today, and a row would orphan on re-seed); granting `studio` `spaces.write` for one button; a `confirmed` column or a `duplicateOf` field on `projects`; editing the ~76 `Button icon=` and ~135 `glyph=` call sites one by one; a `renovacion` stage phrase (it swallowed a quotation); registry tag names as S-12 filter options (zero-match noise); the archived project files in the K-05 Assets tab (2 000 lazy rows in an unpaginated table); putting the open questions for the founder back into a changelog file
files: apps/hub/vite.config.ts, apps/hub/src/auth/permissions.ts (unchanged; gate moved to archive.curate in the archive module), apps/hub/src/data/{archiveFiles,archiveRows}.ts (new), apps/hub/src/data/schema/projects.ts, apps/hub/src/data/seed/{index,archive,projects,assets}.ts, apps/hub/src/design/useFocusTrap.ts, apps/hub/src/domain/archive.ts, apps/hub/src/components/atom/Icon/{Icon.tsx,Icon.meta.ts,iconMap.ts,glyphNode.tsx}, apps/hub/src/components/atom/Button/{Button.tsx,Button.meta.ts}, apps/hub/src/components/atom/Input/Input.css, apps/hub/src/components/molecule/{StatTile,EmptyState}/*, apps/hub/src/components/molecule/PostCard/*, apps/hub/src/components/organism/{Drawer,Modal}/*.meta.ts, apps/hub/src/components/organism/DocumentViewer/*, apps/hub/src/modules/archive/{ArchiveBrowserPage,ProjectPortalPage,model,specs,strings}.ts(x), apps/hub/src/modules/sets/* (new), apps/hub/src/modules/founder/{ArchiveReviewPage.tsx,archiveReview.css,index,specs,strings}.ts(x), apps/hub/src/modules/brand/{DocumentsPage.tsx,documents.ts,brand.css,specs,strings}.ts, apps/hub/src/modules/spaces/{SpacesPage,PostPage,CatalogPage}.tsx + specs / strings / css, apps/hub/src/app/shells.tsx, apps/hub/src/modules/work/NewProjectPage.tsx, apps/hub/src/modules/founder/LeadsPage.tsx, apps/hub/src/work/sample.ts, scripts/archive/build-index.mjs, docs/archive/{index.json,README.md}, docs/archive/projects/*/index.json (186 chunks), apps/hub/public/archive/<slug>/{thumbs,pages}/ (164 new folders), docs/pages/{S-12,S-13,P-06,A-09,G-08,K-01,K-02,K-03,K-05}.md, docs/screenshots/{P-06,A-09}/, docs/knowledge/archive.md, docs/reference/surfaces.md, docs/design/icons.md, docs/README.md, docs/build-plan.md, docs/kanban.md, docs/plan/plan.json, docs/decisions.md, docs/qa/0005-step14-pass3.md, docs/prompts/0020-continue-until-complete.md, apps/hub/src/modules/README.md, package.json, apps/hub/package.json
codes: S-12, S-13, P-06, A-09, G-08, K-01, K-02, K-03, K-05, HUB-01, D-02
model: Fable 5.1 (ar-19 store architecture, integration, permission and chunk-naming decisions, ar-07 index / serve / seed, QA judgement), Opus 5 (ar-14 + ar-10 sets, ar-23 archive review, ar-17 viewer, ar-20 / ar-21 / ar-22), Sonnet 5 (ar-07 crawls and renders of the 2019-2025 and root folders)

# 0022 - Step 14 pass 3: archive data for 164 folders, client sets, founder review, shared viewer

Justin, in the #past-projects thread (prompt 0020): "Continue until complete". Changelog 0021 section H had left eight tasks open on step 14; this pass closes all of them but the two that need credentials (ar-12 storage seam, ar-13 Dropbox API) and the two that need the founder (ar-08 her answers, now on a page; ar-24 the Spanish review). Five workers ran in parallel (drafts merged from `docs/changelog/_pending/`: sections A-E), three crawl / render sessions did the ar-07 year batches (F), and this integration pass added the shared-code changes the drafts asked for, the decisions (G), the open questions for Justin (H) and the QA matrix (I).

## A. ar-19 — archived files as lazy chunks + patch rows, never seeded (Fable 5.1)

**Why.** After ar-06 every archived file was a seeded `assets` row (2 026 rows) with a `belongs-to` relation (2 028 rows) and the 19 deep indexes were eager-imported: `localStorage['aluzina.data']` sat at 3 659 382 chars against the 4.5-5 MB browser line and the main bundle chunk at 2.9 MB (0021 A, D-070 "known cost"). ar-07 adds ~2 500 files for 167 more folders; at that row shape the store overflows.

**Design (D-071).**
1. **One lazy JSON chunk per project, none in the seed.** `scripts/archive/build-index.mjs` writes `docs/archive/projects/<slug>/index.json` for **every** non-admin inventory folder (the deep indexes unchanged; the shallow ones at `depth: 1` holding the folder's direct child files in the same `DeepIndex` shape) and strips `children` from `docs/archive/index.json` (726 KB -> 150 KB; kept: `fileCount`, `dirCount`, `extensions`, `latestModified`, `deepIndex`, `kind`, `note`; added: `depth`, `cover: { assetId, thumb, pages? } | null`, `folders` for the featured notes; the one admin folder keeps its children because they seed as posts). **Guard**: a deep index that `render-previews.py serve` already rewrote (`served` block, D-069) is kept verbatim (only `indexedAt` / `depth` refresh) — `--reindex-deep` regenerates it; the first run of this pass clobbered the served copies and the guard is what made the 19 byte-identical again. R7 now also greps the chunks.
2. **Schema**: `Project` += `fileCount: number | null`, `coverUrl: string | null`, `archiveSlug: string | null`, `fileTypes: FileType[]` (the S-12 mosaic for the projects without a cover needs the type distribution, computed at seed from `extensions`). All four `Project` literal sites got the null / `[]` values (`seed/projects.ts`, `seed/assets.ts`, `modules/work/NewProjectPage.tsx`, `modules/founder/LeadsPage.tsx`, `work/sample.ts`).
3. **Seed** (`seed/archive.ts`): projects, spaces, year areas, tags, the featured note (now folders by stage with file counts, from `folders`) and the `for-client` / `produced-by` / `references` relations stay; **no file `assets` rows and no per-file relations**. `coverAssetId` from the inventory's `cover.assetId`, `coverUrl` from `cover.thumb`. Company documents (`seed/company.ts`) stay the only seeded `kind: 'file'` rows (12). `SEED_VERSION` 9 -> 10 (-> 11 in section F): **tags / stages saved on archived files before this pass are lost on re-seed** (they lived on seeded rows).
4. **Pure row builder** `apps/hub/src/data/archiveRows.ts`: `rowsFromDeepIndex(slug, index, year): Asset[]` with the seed's exact rules (ids `ast-ar-<slug>-<slugify(name)>`, `-2`… on collisions; `stageFor`; served renders; `SEED_AT` timestamps) and `archiveAssetIds(slug, files)`. The id rule is ported to `build-index.mjs` (`rowIds`) for `cover.assetId`; a node check confirmed 2 026 rows over 186 chunks, 18 / 18 cover ids equal, 0 duplicate ids. **Fixed on the way**: the old rule could re-issue an id when `-2` collided with a file literally named "… 2" (5 folders); both copies now count until free.
5. **Loader + hook + patch helper** `apps/hub/src/data/archiveFiles.ts`: `import.meta.glob('@docs/archive/projects/*/index.json', { import: 'default' })` (lazy) keyed by slug; `loadProjectFiles(slug, year)` cached per slug; `useProjectFiles(project)` = chunk rows overlaid by `useTable('assets', { where: { kind: 'file' } })` rows of the same id (edits win, re-render on `subscribe`); `saveFilePatch(data, baseRow, patch, projectId)` = `update(id, patch, { basedOn })` when stored, else `create('assets', row, id)` **with the chunk row's id** plus a `belongs-to` relation (D-026) so S-12 can count curated files.
6. **Module**: S-12 reads `fileCount` / `coverUrl` / `fileTypes` from the row (no asset scan); "N tagged" counts the project's curated rows; the Tag filter's options are `projects[].tags` ∪ tags on curated rows. S-13 uses `useProjectArchiveFiles` (sorted `useProjectFiles`), `Skeleton` while the chunk loads, `EmptyState` with the reason on error; `tagFile` / `setStage` go through `saveFilePatch`; `setCover` writes `coverAssetId` + `coverUrl`.
7. **Chunk names** (integration): `apps/hub/vite.config.ts` gains `build.rollupOptions.output.chunkFileNames`, so the lazy chunks are `assets/archive-<slug>-<hash>.js` instead of 186 `index-<hash>.js` (verified in `dist/`: `archive-hoy-*.js` 136 kB, `archive-joe-gallina-interior-*.js` 139 kB, shallow ones 1-5 kB).

### Numbers (built app, headless Chromium against `npm run preview`, studio, fresh profile)

| | Before (0021) | After ar-19 |
| --- | ---: | ---: |
| `localStorage['aluzina.data'].length` | 3 659 382 | **847 459** |
| assets rows / chars | 2 096 / 2 068 995 | 70 / 64 012 (12 file rows) |
| relations rows / chars | 2 913 / 890 885 | 393 / 109 663 |
| main chunk `index-*.js` | 2 911 867 B | **1 960 193 B** (2 021 kB after this pass's pages) |
| JS chunks in `dist/assets` | 188 | 375 (186 lazy project chunks) |
| `docs/archive/index.json` | 726 157 B | 149 727 B |
| S-13 HOY (206 files) open: first / warm / fresh profile | 308 / 227 / 1 189 ms | 208-308 / 43-90 / 520 ms |

Verified: S-12 205 projects, shelves 3 / 23 / 179, 18 covers, 155 mosaics, 0 overflow at 390 / 1280; S-13 joe-gallina 179 files / 9 stages, drawer page render HTTP 200; tag a file -> stored row + relation, visible after reload and live in a second tab; set cover -> `coverAssetId` + `coverUrl`, hero and S-12 card change; G-08 as brand unchanged; 0 console errors.

## B. Small fixes — ar-20 focus trap, ar-21 glyphs, ar-22 HOY stages (Opus 5; integration Fable 5.1)

**ar-20 (D-072).** `design/useFocusTrap.ts` held `onClose` in its effect's dependency list, so any caller whose `onClose` identity changed while the dialog was open tore the effect down and re-ran it, and the re-run's opening `focus()` pulled focus back to the close button. The fix is one hook: `onCloseRef.current = onClose` and `useEffect(…, [ref, active])`. Focus checks on W-02 `TaskDetailDrawer` (typing stays in the field), K-03 "File in…" (ticking a checkbox stays on it), O-03; **negative control** against the old dependency array reproduced the bug on K-03 exactly. Integration: the S-13 `useCallback` workaround of ar-09 is removed (the Escape effect depends on `patch` now) and `Drawer` / `Modal` metas say a stable `onClose` is no longer required.

**ar-21 (D-073).** A **string** `icon` / `iconEnd` on `Button` resolves as an icon name first, then through `GLYPH_ICONS`, then renders as text — D-064's order one level lower — so the 76 `Button icon="…"` sites draw icons with no module edit. K-01 space heading and rows (`SpaceGlyph`: `SPACE_KIND_ICONS[kind]`, then the seeded glyph, then text), `PostCard` (`pin` icon, `POST_KIND_ICONS` beside the kind badge), the shell theme toggle / menu / "Browse spaces" (`moon` / `sun` / `menu`). 7 new icons (`chevron-left`, `moon`, `sun`, `pin`, `print`, `flag`, `menu`): 71 -> **78**; 20 new `GLYPH_ICONS` rows. Integration took the draft's proposal: the resolver moved to `components/atom/Icon/glyphNode.tsx` and **`StatTile` and `EmptyState` resolve `glyph` the same way** (135 sites in 52 files, including the four archive `EmptyState`s `▤` / `⌕`), at size `xl` for the empty state. Left as typography: `›` breadcrumb separators, `·`, `…`, the `▸ ▾` sort chevrons in `WorkList` / `DataTable`.

**ar-22.** `STAGE_PHRASES` in `domain/archive.ts` gained the 2026 folder template (`01 FOTOGRAFIA Y VIDEO DEL ESPACIO` -> survey, `03 PLANOS DEL ESPACIO` / `DETALLES TECNICOS` -> technical, `04 COTIZACION DEL ESPACIO` -> quotation, `05 CRONOGRAMA DE OBRA` / `07 FOTOGRAFIAS DE OBRA Y AVANCE` -> execution, `08` / `09` / `PAGOS` / `FACTURAS` / `INFORMES` -> admin, `CERTIFICADOS Y GARANTIAS` -> delivery, `MANUAL DE MARCA` / `INFORMACION DISEÑO INTERIOR` / `ARTES` -> design-development, `IMAGENES` / `VIDEOS` / `ECOSISTEMA VIRTUAL` -> marketing, last in the list); each phrase carries a comment naming its folder. `renovacion` was tried and dropped (it swallowed SODIME's quotation). HOY 180 of 206 files in "other" -> **0**; CARTAGENA COPETRAN 10 -> 0; SODIME 10 -> 1; every deep index 1 475 -> 1 183. Stage is computed at load, so no data regeneration. The table is now in `docs/knowledge/archive.md`. **Question for Justin in H**: `IMAGENES HOY`.

Playwright (390 / 1280, light / dark): HUB-01, K-01, K-03, W-02, O-03 — 20 / 20 cells, 0 console errors, 0 px overflow, the DOM query for the 41 audited glyph characters over `.btn`, `.pcard`, `.spaces-h2`, `.shell-controls`, `h1`, `nav` returns nothing.

## C. ar-14 + ar-10 — P-06 client example set and "Save set to Spaces" (Opus 5)

**P-06 `/sets`** (new module `modules/sets`, surface `public`, shell `bare`, no permission). The set is **stateless in the URL** (D-074): `/#/sets?p=<projectId>,<projectId>&t=<title>&lang=es|en&print=1`. One section per project in the order given — cover (`Thumb` 16:9 from `coverUrl`, else a `FileIcon` mosaic from `fileTypes`), name, year · type · location, tags, the client-facing sentence, and a strip of up to 8 design previews from `useProjectFiles(project)` that open in a `Drawer` + `DocumentViewer`. **Spanish by default** (D-076), ES | EN toggle from its own string table, "Print / Save as PDF" (`window.print()` once when `print=1`), "Copy link", a print stylesheet (one project per sheet, controls gone, black on white, previews as a 4-column grid). Actions `sets.print`, `copyLink`, `setLang`, `openPreview`, `closePreview`.

**Privacy and tone (D-075).** `clientPreviews` keeps only files with a served thumbnail that are neither tagged `confidencial` nor redacted by the crawler — a confidential file is never rendered and its **name is never printed**. `clientSummary` keeps whole sentences and drops the crawler's provenance and every "inferred; confirm with the founder" note; when nothing client-facing is left the paragraph is omitted (the case for every archived project until ar-08). Location prints only when it is not the seed placeholder; tags drop `archive` / `dropbox` / a bare year / the project type.

**S-12 set bar is real.** "Open example page" and "Export PDF" are anchors to P-06 (`&print=1` for the PDF). "Save set to Spaces" writes a `posts` row (`kind: 'link'`, the P-06 link, a markdown list of `name · year · type`, tags `portfolio` + `ejemplos`, `status: 'published'`) filed through `filings` in `sp-portfolio` and `sp-archive`, and the bar then offers "Open the saved set". `archive.exportSet` is real; `archive.openSetPage` and `archive.saveSetToSpaces` are new; no `Placeholder` left in the bar. **Integration (D-077)**: the draft gated saving on `spaces.write`, which the studio does not hold, so Sarai saw no button; the gate is now `archive.curate` (studio, brand, founder) — the provider checks no permission, so the page gate is the whole rule and studio's grants are unchanged.

Verified: P-06 at 360 / 390 / 768 / 1280 / 1920 / 2560 / 3840 with three projects: 3 sections, 3 covers, 24 previews, 0 px overflow, 0 console errors, no target under 44 px, ES default, `lang=en` in English; preview Drawer opens the paged viewer (5 pages on HOY's technical PDF), Escape closes; print emulation: controls hidden, `break-before: page` per project; empty `/#/sets` renders the "this link has no projects" card. S-12 as founder: 3 ticked -> bar with 0 placeholders, both links carry the ids in pick order, "Save set to Spaces" -> "archivado en 2 espacios", the post opens on K-03 with the working link. Screenshots `docs/screenshots/P-06/{en-390,es-1280,en-1920,print-1280}.jpg`.

## D. ar-23 — A-09 Archive review for the founder (Opus 5)

`/founder/archive-review`, permission `projects.write`, nav group `projects`, order 12: the facts the intake inferred (D-060, changelog 0019 H) are confirmed or corrected **in the product** instead of in a chat thread. Seven columns with three inline editors (type, status, year), a client picker with "create client" in a Drawer, "Mark as duplicate of…", bulk "Confirm selected", a "pending only" view, and a filter button per open question. Eight actions (`founder.confirmProject`, `setProjectType`, `setProjectStatus`, `setProjectClient`, `setProjectYear`, `markDuplicate`, `createClient` (`clients.write`), `confirmSelected`), all registered while mounted.

Decisions (G): confirmation = tag `confirmado` + the inferred sentence removed from the summary (D-078); duplicate = `replaces` relation + tag `duplicado` (D-079); the eleven open questions live in the page as `OPEN_QUESTIONS` (D-080); a `clients` row is created from the row that needs it (D-081, the first place in the hub that creates a client). **Library fix taken (integration)**: `.field` in `components/atom/Input/Input.css` gets `position: relative`, so a `hideLabel` label (`.visually-hidden`, absolute) no longer lays out against the page inside a scrolling table (72 px of page overflow at 1280 before A-09 worked around it); verified after the change on W-03, A-09, O-12 and S-13 at 390 and 1280: 0 px overflow, the `.field__prefix` offset unchanged (12 px, its containing block is `.field__control`), every hidden label inside its field, 0 console errors.

Verified as the founder at 390 and 1280: type / status / year changed on one row, a client created and assigned, the row confirmed (left the pending view, stored row carries `confirmado` with the sentence gone), a bulk confirm, a duplicate marked (relation written), S-12 as studio shows the new type and status; 0 console errors, no page overflow (the table scrolls in the DataTable's own scroller at a 66 rem minimum). Screenshots `docs/screenshots/A-09/`.

## E. ar-17 — G-08 and Spaces use the shared `DocumentViewer` and `Thumb` (Opus 5)

- **G-08 `/brand/documents`**: the page's own `DocFrame` (`<object>` / `<iframe>` chain) is gone; the viewer Card is the shared `DocumentViewer` fed by `viewerAssetOf(doc)`, the document cards gain a `Thumb` (`ratio="3:4"`, badge `PDF`), the `.brand-doc__frame` rules are deleted; 7 declared actions, unchanged. A row that later gains served renders becomes the paged image viewer with no page change.
- **`PostCard`**: `thumbType` / `thumbSrc` / `thumbLabel` props draw a `Thumb` beside the title inside the one `<button>` (no second tab stop).
- **K-01 / K-02**: a `file` post shows the served thumbnail of the first related `assets` row, else the file-family icon from `fileTypeOf(sourceName ?? title)`.
- **K-03**: a `file` post gets a file block (Thumb, type, page count) and a **Preview** button opening the `DocumentViewer` in the page's Drawer when the file can be shown in place; otherwise the link stays and a sentence says why. `spaces.previewAsset` / `spaces.closeAssetPreview`.
- **K-05**: a fifth tab **Assets** — the `assets` rows that are files in their own right (`kind != 'page'`): 14 today (2 marketing PDFs + 12 company documents), columns Thumb / name / kind / file type / pages / source / relations / Open; row activation previews in the Drawer; `spaces.catalogTab` += `assets`, `spaces.openAssetSource` declared. Archived project files are deliberately not there (they are lazy chunks and S-12 / S-13 are their browser).
- `usages` lists of `DocumentViewer`, `Thumb`, `FileIcon` updated so `/dev/components` tells the truth.

Playwright 390 / 1280, light, EN — 8 / 8 cells (G-08 brand, K-05 `?tab=assets` studio, K-02 `sp-ar-admin`, K-03 file post): 0 console errors, 0 failed requests, 0 px overflow; the viewer really renders a page image (`./archive/company/pages/bombilla-aluzinogena-page-01.jpg`, 1200 × 927, "Page 1 of 3") on both G-08 and K-05; Spanish headers "Archivos 14", "Tipo de archivo" (the first draft used `Tipo` twice). Found, not fixed (kanban): the `SpaceTree` chevron is a 28 px target; no seeded `file` post points at an `assets` row; P-05 still has its own `DocFrame`; duplicate viewer label keys.

## F. ar-07 — deep indexes and served previews for the 2019-2025 and root folders (Sonnet 5 crawls and renders, Fable 5.1 index / serve / seed)

**Crawls and renders (Sonnet 5, three sessions, scratchpad only).** Full-depth Dropbox crawl (`crawl-dropbox.mjs --depth=8`, 2-3 s per page, 60-90 s back-off, one retry): **0 login gates, 0 rate limits, 0 crawl errors** over the 164 folders (older batch: 2021 23 folders, 2020 25, 2019-2023 13 + JOE GALLINA already deep, root 2 — 2 478 files; 2025: 37 folders, 624 files; 2024: 43 folders, 573 files; 2022: 25 folders, 862 files). Downloads kept to the D-058 allow-list (design folders and loose top-level files; the administrative / quotation / invoice / payment folders never downloaded) within 500 MB caps per batch, originals deleted after each folder; the 2025 and 2024-2022 batches needed two and three passes because the cap ran out (and one interrupted pipeline rewrite emptied the 2025 render output once — the summaries record it). Renders: thumbnails at 512 px and pages at 1000 px from the renderer; **render errors** 20 (older) + 4 (2025) + 33 (2024-2022): `.mov` / `.MOV` files where ffmpeg produced no frame and a few Office files LibreOffice could not convert; the files keep their rows without a preview. **`el-cortijo-cliente-ivan` (2025) has no previews at all**: its 5 eligible files are `.mov` renders that ffmpeg could not frame (codec support) — a renderer follow-up, not a policy exclusion.

**Index (Fable 5.1).** `build-index.mjs` run with the 164 render indexes as `--deep` (+ `--deep-entries` for the top-level folder links): **183 deep chunks + 3 shallow** (the empty folders) under `docs/archive/projects/`, inventory unchanged at 187 entries. Fixes made on the way, all in the script: (1) a deep index now matches its inventory entry on **collapsed whitespace** and keeps the inventory's spelling (`0_80 OZAOZ  jinetes del horizonte` and `Proteccion Anticontagio  ALUZINA` had spawned second entries with a `-2020` id); (2) `yearFolderOfUrl` returns `root` for a folder next to the year folders (LIFE VIOLETA VILLA, SANTIAGO AGUIRRE ILUMINACION root, id `santiago-aguirre-iluminacion-root`) instead of defaulting to `2019-2023`; (3) **R1 gains person names**: a file named after a person of the R4 table is a personal document — `MARTA OVIEDO BALCON.pdf` (BALCONES ALHAMBRA) and two `Links/Daniel Yepes.jpg` (AARON, HOTEL SPA MEDELLIN) had made R7 exit 1, and the rule was fixed, not the check; (4) **modal-year inference** for a deep-indexed folder in a range / root folder (JOE GALLINA INTERIOR 2023 from 170 of 179 dated files, LIFE VIOLETA VILLA 2019 from 10 of 14, SANTIAGO AGUIRRE root 2025), `yearInferred: true` and the note says so; (5) **R7 now also checks every served thumb / page file name** (de-dashed, against the D-058 pattern, the person names and the person segments the index maps to roles), because the renderer slugs the folder path into the file name: it found **no leak** — the only hits were two Dropbox "conflicted copy" names in `urgent-care` carrying the founder's name, which is public and exempt everywhere in R7. Final self-check: `privacyCheck.ok: true` over 1 716 inventory entries, 5 291 chunk files and 2 527 served file names; 1 107 files redacted in all (by name / folder / content), 12 folder segments rewritten.

**Served renders (D-082).** `render-previews.py serve` per folder (6 pages per file, thumbs <= 640 px q80, pages <= 1 200 px q72 unless trimmed), then `build-index.mjs` again so `cover` and the per-folder counts read the served files (the served guard kept the 19 earlier indexes byte-identical but for `indexedAt`).

| year folder | folders | files indexed | deep | with served renders | thumbs | pages | served MB | covers | redacted files |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 2026 | 18 | 575 | 18 | 18 | 408 | 150 | 23.52 | 17 | 105 |
| 2025 | 37 | 624 | 35 | 35 | 381 | 71 | 11.81 | 25 | 55 |
| 2024 | 43 | 573 | 43 | 43 | 190 | 125 | 11.76 | 29 | 170 |
| 2022 | 25 | 862 | 24 | 24 | 391 | 39 | 11.64 | 17 | 180 |
| 2021 | 23 | 646 | 23 | 23 | 159 | 143 | 11.61 | 17 | 268 |
| 2020 | 25 | 1045 | 24 | 24 | 167 | 93 | 10.58 | 14 | 117 |
| 2019-2023 | 14 | 788 | 14 | 14 | 71 | 122 | 10.64 | 12 | 207 |
| root | 2 | 178 | 2 | 2 | 10 | 7 | 0.50 | 2 | 5 |
| **total** | **187** | **5291** | **183** | **183** | **1777** | **750** | **92.06** | **133** | **1107** |

The three folders without a deep index are the three empty ones (LONDON CITY BARBER SHOP 2020, one 2022 and one 2025 folder with no files); they keep a `depth: 1` chunk with no files. Deep-indexed in 0019: JOE GALLINA INTERIOR; in 0021: the 18 of 2026; in 0022 (ar-07): the other 164.

Trims, largest page sets first, every thumbnail kept: **2025** 16.12 -> **11.81 MB** — pages dropped on `proyecto-talita` (30 pages, 1.34 MB), `urgent-care` (25, 0.95), `stiven-propiedad-305` (2, 0.08), `casa-rustica-iluminacio` (12, 0.79); thumbs re-encoded at q65 on the first three (photo sets of 67-82 thumbs, already 512 px from the renderer, so a smaller pixel size would have changed nothing); every other 2025 page at 1 000 px q68. **2024** 19.88 -> **11.76 MB** — pages dropped on `medellin-magia-naturaleza-y-raiz` (63 pages, 5.0 MB), `casa-vitall` (33, 1.77), `casa-jorge-y-ligia` (21, 1.35). **2022** 18.06 -> **11.64 MB** — pages dropped on `armoniko-interior` (12, 1.15), `cabana-amaga` (23, 1.07), `dota-hogar` (26, 0.95), `casa-miami-ovy` (19, 0.82), `natasha-capilar` (8, 0.62), `brookling-pizza` (8, 0.53), `luminarias-para-puro-cuero` (6, 0.47), `tg-novias` (5, 0.44), `rl-exotics-miami` (4, 0.38). 2021 (11.61), 2020 (10.58), 2019-2023 (+4.54) and root (0.50) needed no trim. **62.4 MB new served** (the ~60 MB guide, 4 % over, taken rather than losing thumbnails), 92.1 MB in total under `apps/hub/public/archive/`, `dist/` 251 MB. **Cost named**: a PDF whose pages were dropped opens the S-13 drawer in its `doc-viewer--none` state (thumb in the grid, nothing to page through) — seen on `casa-rustica-iluminacio` in QA; a viewer fallback that shows the thumbnail large with "Open at source" is a follow-up (kanban).

**Seed and app.** Nothing in the app changed for ar-07 (D-071 did the work): the 164 new chunks are picked up by the glob and named `assets/archive-<slug>-<hash>.js`. `SEED_VERSION` 10 -> **11** because the project rows change values (`fileCount`, `coverUrl`, `fileTypes`, `year` for three folders; covers 18 -> 133): **the re-seed drops any tag / stage / cover edit and any A-09 confirmation stored on version 10** — none exist outside this pass's own tests. S-12 now shows 133 covers and 54 mosaics; `docs/knowledge/archive.md` carries the table above and the per-year inventory.

## G. Decisions

D-071..D-082 as listed in the header; full rows in `docs/decisions.md`. In one line each: files are lazy chunks + patch rows (D-071); the focus trap holds `onClose` in a ref (D-072); string glyphs resolve inside `Button` / `StatTile` / `EmptyState` (D-073); a set is a stateless link (D-074); confidential gets no preview and no mention outside the studio (D-075); client pages default to Spanish and translate themselves (D-076); `archive.curate` gates set saving (D-077); confirm = tag + summary edit (D-078); duplicate = `replaces` relation (D-079); open questions are page data (D-080); clients are created from A-09 (D-081); the 2019-2025 render budgets (D-082).

## H. Open questions for Justin (and the founder, on A-09)

1. **`IMAGENES HOY`, marketing or delivery?** HOY 2026 keeps 105 camera-dated photos in `IMAGENES HOY` and a separate `01 FOTOS Y VIDEOS DEL ESPACIO` for the survey. ar-22 files `IMAGENES …` as **marketing** (the library a project publishes from); if those photos are the final-photo record of the finished space, the stage should be **delivery** and the phrase moves. One word from you or Alejandra decides it.
2. **Covers for clients.** HOY's cover is a technical sheet, so its P-06 section opens on paperwork; choosing an image cover for the 133 covered projects (S-13 "Set as cover") would lift the whole client page. Who picks — Sarai, or the founder while she reviews on A-09?
3. **One client-facing sentence per project** (ar-08). P-06 prints no summary for archived projects until someone writes one; the review page is where the founder can, one row at a time.
4. **`WhatsApp Image …` site photos** (carried from 0021 H): still hidden by D-059 R1; they are most of the 2019-2025 photo record too.
5. **Credentials** for the two remaining tasks: a Supabase project (ar-12, storage seam + auth gate) and a Dropbox app token (ar-13, API connector replacing the headless crawl).
6. **JOE GALLINA INTERIOR's year.** It sits in the range folder "2019 2023", so the folder gave no year; the index now infers **2023** from its file dates (170 of 179) and says so in the note. Confirm on A-09 or correct it there.
7. **Pages before thumbnails.** The 2022 / 2024 / 2025 trims removed page renders from 16 folders to stay at ~12 MB per year; if you would rather see fewer projects with all their pages than every project with thumbnails only, say so and the serve budget flips (D-082).
8. **SANTIAGO AGUIRRE ILUMINACION twice**: a root-level folder (10 files, 2025 dates) and `05 SANTIAGO AGUIRRE ILUMINACION` inside 2025 (8 files). Same project? A-09's "duplicate of" answers it.

## I. QA — `docs/qa/0005-step14-pass3.md`

Playwright (Chromium at `/opt/pw-browsers`, headless) against `npm run preview` of the final build (version 0.14.0, SEED_VERSION 11), light, English unless the page decides otherwise. **17 / 17 cells pass**: 0 console errors, 0 failed requests (no response >= 400, no `requestfailed`), 0 px horizontal overflow, no target under 44 px except the A-09 checkboxes, whose `<input>` is 24 px inside the library's 44 px `<label>` (the same measurement note as ar-17). Route manifest of the build: **123 routes, 1 055 declared action entries, 403 distinct ids**.

| cell | width | h-overflow | console errors | failed requests | smallest target | what it showed |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S-12 | 390 | 0 px | 0 | 0 | 44 px | 133 archive images, 186 project links |
| S-12 | 1280 | 0 px | 0 | 0 | 44 px | 133 archive images, 186 project links |
| S-13 2025 casa-rustica | 1280 | 0 px | 0 | 0 | 44 px | 9 archive images, 6 project links |
| S-13 2024 light-alchemy | 1280 | 0 px | 0 | 0 | 44 px | 13 archive images, 6 project links |
| S-13 2022 cabana-amaga | 1280 | 0 px | 0 | 0 | 44 px | 55 archive images, 6 project links |
| S-13 2021 modulos-biotectura | 1280 | 0 px | 0 | 0 | 44 px | 31 archive images, 6 project links |
| S-13 2020 ozaoz | 1280 | 0 px | 0 | 0 | 44 px | 21 archive images, 6 project links |
| S-13 2019-2023 thanal | 1280 | 0 px | 0 | 0 | 44 px | 14 archive images, 6 project links |
| S-13 root life-violeta-villa | 1280 | 0 px | 0 | 0 | 44 px | 8 archive images, 6 project links |
| S-13 2025 proyecto-talita (thumbs only) | 390 | 0 px | 0 | 0 | 44 px | 68 archive images, 6 project links |
| P-06 | 390 | 0 px | 0 | 0 | 44 px | 27 archive images |
| P-06 | 1280 | 0 px | 0 | 0 | 44 px | 27 archive images |
| P-06 | 3840 | 0 px | 0 | 0 | 88 px | 27 archive images |
| A-09 | 1280 | 0 px | 0 | 0 | 24 px | 133 archive images |
| A-09 | 3840 | 0 px | 0 | 0 | 48 px | 133 archive images |
| G-08 | 1280 | 0 px | 0 | 0 | 44 px | 5 archive images |
| K-05 assets | 1280 | 0 px | 0 | 0 | 44 px | 5 archive images |

S-13 drawer follow-up (1280, studio): clicking a file thumbnail opened the `Drawer` on every year's sample folder and the image it showed answered **HTTP 200** — `ozaoz-jinetes-del-horizonte` (2020, image viewer), `modulos-biotectura-para-interiorismo` (2021, **paged viewer**, `pages/propuesta-aluzina--modulo-page-01.jpg`), `cabana-amaga` (2022), `light-alchemy` (2024), `thanal` (2019-2023), `life-violeta-villa` (root); `casa-rustica-iluminacio` (2025) opened in the `doc-viewer--none` state because that folder's pages were dropped (section F). Every served render referenced by those seven indexes was fetched: **223 / 223 HTTP 200**. P-06 was exercised with `prj-ar-hoy`, `prj-ar-modulos-biotectura-para-interiorismo`, `prj-ar-medellin-magia-naturaleza-y-raiz` (27 archive images: 3 covers + 24 previews) at 390 / 1280 / 3840. Full matrix: `docs/qa/0005-step14-pass3.md`.

## J. Verification of the integrated tree

`npm run typecheck && npm run build` green (the known chunk-size warning on the main chunk only); `dist/` holds 186 `assets/archive-<slug>-*.js` chunks. R7 privacy self-check passed over the inventory, the deep indexes and the chunks (`privacyCheck.ok: true` in `docs/archive/index.json`). Pages deploy: pushed to `main`; the Pages run and a live thumbnail check are recorded in prompt 0020's response.

## K. Deferred / next (plan.json step 14)

- **ar-08** the founder answers the open questions on A-09 (next; owner founder), then the seed fix from her answers.
- **ar-24** Spanish review of S-12 / S-13 / P-06 / A-09 with the founder (the ar-18 remainder).
- **ar-12** storage seam + auth gate and **ar-13** Dropbox API connector — blocked on credentials (H 5).
- Follow-ups filed on the kanban: `SpaceTree` chevron target, P-05 `DocFrame`, viewer label keys, a seeded file post with an `assets` relation, "cover for clients", the clients review table and a "not a project" action for container / empty folders.
