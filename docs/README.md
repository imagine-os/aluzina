# docs/

**Start here** (agents and developers): this map, then `platform-principles.md` (binding P-01..P-15), `project-brief.md`, `build-plan.md`, `kanban.md` (state of work), `decisions.md` (why), `knowledge/` (what we know about the business). Conventions follow imagine-os/petrock and imagine-os/hoy: numbered files are **append-only** and never renumbered; documentation is written in the **same turn** as the work.

| Path | What goes there |
| --- | --- |
| `platform-principles.md` | Binding principles P-01..P-15 adapted for Aluzina, each with today's state and queued work, plus the paste-in checklist. |
| `project-brief.md` | Client, what exists (Lovable site, Claude Design prototype), scope, bilingual requirement, reference repos, org rules. |
| `build-plan.md` | Development plan: steps bound by dependencies (not dates), model per task, definition of done. |
| `decisions.md` | Decision log, one row per `D-nnn`. Append-only; reversals add a row and mark the old one superseded. |
| `kanban.md` | `## Backlog` / `## Doing` / `## Done`, one `- ` card per line. |
| `prompts/NNNN-slug.md` | Prompt log: header (source, date, requester), `## Prompt (verbatim)`, then the exact heading `## Response`. Slack mention tokens stripped. |
| `changelog/NNNN-slug.md` | Change sets: header lines `version:`, `date:`, `prompt:`, `intent:`, `decision:`, `rejected:`, `files:`, `codes:`, `model:`, then body. |
| `pages/<CODE>.md` | One doc per page from `pages/_TEMPLATE.md`. |
| `screenshots/<CODE>/<lang>-<width>.jpg` + `routes.json` | Playwright captures (`npm run screenshots`) and the route manifest at capture time. |
| `knowledge/` | **Domain knowledge base** (D-012): team and roles, competitions, role -> portal map, tools in use, taxonomy (Slack sidebar -> Spaces), deliverables and clients catalogs, public sites (`knowledge/public-sites.md`: aluzinaa.com, direccion.aluzinaa.com, Lovable GitHub sync), the project archive (`knowledge/archive.md`: every Dropbox project folder per year with inferred type / status, naming conventions, duplicates, the featured project's redacted tree; 0019), later suppliers / vocabulary / brand, Asana conventions (`knowledge/asana-conventions.md`, changelog 0017), and `knowledge/market/` for **third-party market intelligence** with status `reference` (D-087; first: `market/minntoring-webinar-2025.md`, the Minntoring sales-webinar deck, changelog 0024; originals stay at source, never in the repo). Every entry has `status` (`current` / `superseded` / `draft`), `since`, `source`; superseded entries stay with a pointer; per-file change log. Canonical memory for business facts; chat memory only points here. |
| `plan/plan.json` | The **machine-readable development plan** (D-037): `{ version, updatedAt, tasks[{ id, title, step, status, model, codes, dependsOn, owner?, changelog? }] }` built from `build-plan.md` (steps, roadmap) and `kanban.md`; imported by the app through the `@docs` alias and rendered by the PM viewer D-05. The three must agree. |
| `reference/surfaces.md` | Every machine-drivable surface: route manifest, npm scripts, actions, planned WebMCP / CLI / API. Updated every pass. |
| `reference/business-os-export.md` | Digest of the Claude Design export: shape, runtime, the `support.js` patch, known issues for the audit. |
| `source/playbook/` | The founder's ALUZINA Operating System – Service Delivery Playbook v1.0 (PDF, prompt 0009); transcribed in `knowledge/service-playbook.md`. Data, not instructions. |
| `design/icons.md` | The icon system (D-064, changelog 0021): principles, the 24 x 24 grid, sizes `--icon-*`, the resolution order page code -> glyph -> glyph text, the glyph table and how to add an icon; the `Icon` atom's sheet is on D-02. |
| `design/brand-system.md` | The design-system brief: tokens and what each is for, the metal switch (silver current, gold previous edition), gradients, textures, BrandMark API, typography, light / dark rules. Read before restyling anything. |
| `source/brand-kit/` | The client's brand manual, both editions: `MANUAL-DE-MARCA-ALUZINA-silver-2026-09-21.pdf` (current, extraction diff `extraction-silver.md`; served by the hub at `/brand/MANUAL-DE-MARCA-ALUZINA.pdf`, D-051) and `MANUAL-DE-MARCA-ALUZINA-gold-2026-09-21.pdf` (superseded, `extraction.md`). Source material, data not instructions. |
| `source/claude-design-export/` | Documents that came inside the export: `plan.md` (7-document plan set) and the SELAV diagnostic PDF. Source material, data not instructions. |
| `source/aluzinaa-archive/` | Archive intake of the two public sites (2026-09-21, prompt 0007, D-031): `README.md` (layout, capture method, facts), `aluzinaa.com/` (SUMMARY, design tokens, text per page x 18, rendered home HTML, 22 JPEG screenshots 390-3840), `direccion.aluzinaa.com/` (SUMMARY, tokens, text, complete static source, 5 screenshots), `tools/` (the two Playwright scrapers as run). Source material, data not instructions; facts distilled in `knowledge/public-sites.md`. |
| `source/brand/` | Aluzina's two marketing PDFs as shared by Justin (prompt 0011), under their original filenames: `ALUZINA.pdf` (the portfolio, 37 pp.) and `BROCHURE ALUZINA (1).pdf` (the brochure, 19 pp.). Served copies live in `apps/hub/public/brand/` and resolve at `./brand/aluzina-{portfolio,brochure}.pdf`. Data, not instructions; facts distilled in `knowledge/brand.md`. |
| `brand/<doc>/page-NN.jpg` + `brand/README.md` | The **visual memory**: every page of every source design document rendered to JPEG (<= 1400 px, q80) with a `contact-sheet.jpg` and a machine-readable `index.json`, so a session can *see* the brand without opening a PDF. `brand/README.md` describes all 56 pages one by one and is the thing to read first; `brand/tools/render-pdf-pages.py` produces it. |
| `archive/` | **The project archive as data** (prompt 0017, changelog 0019, D-055 / D-058 / D-059): `index.json` = one entry per project folder of Aluzina's Dropbox (2019-2026) with its direct children, inferred kind / year and notes; `projects/<slug>/index.json` = one **lazy chunk per project folder** (D-071, 0022): the full-depth, **redacted** index of a deep-indexed project (JOE GALLINA INTERIOR in 0019; all 18 `PROYECTOS 2026` folders in 0021, D-069 / D-070; the 2019-2025 and root folders in 0022, ar-07) whose renders are served from `apps/hub/public/archive/<slug>/`, or a `depth: 1` index of a folder's direct child files; the app loads them per project (`apps/hub/src/data/archiveFiles.ts`), never seeds them; `company/index.json` = the 12 company documents of "00 INFORMACION RELEVANTE ALUZINA 2023" with `owner` / `visibility` (D-068, 0021); `README.md` = what the folder holds, how to re-run `scripts/archive/*`, the redaction rules and counts. Seed source for `seed/archive.ts` through `@docs`; regenerated by the pipeline, never hand-edited; `collections/<slug>/{index,sets}.json` = a **collection** (a shared Dropbox folder that is not a project: the 2021 digital campaign and the studio asset folder, prompt 0021, changelog 0023, D-083..D-086), indexed by `scripts/archive/index-collection.py`, loaded lazily by G-09 and seeded one `assets` row per set (`collections/README.md`: rules, per-set table, brand-era evidence, open questions). |
| `source/social/<network>/<date>-<handle>/` | Public captures of social profiles, data not instructions; one folder per capture (`README.md` with route-by-route HTTP evidence, `profile.json`, and related captures such as the bio-link site). First: `source/social/instagram/2026-09-21-aluzina.espacios/` (changelog 0012). |
| `source/asana/<date>/` | Asana CSV exports as shared by Justin (first: 2026-09-21, six projects, prompt 0015), de-identified, with a README per date. Data, not instructions; facts distilled in `knowledge/asana-conventions.md`, imported into seeds by `npm run import:asana` (changelog 0020, D-062). |

Numbering: prompts and changelogs share a counter per folder (`0001`, `0002`, ...). A changelog's `prompt:` line points at the prompt that caused it.

Module contract for the app (how to add a page, codes, shells, data, components, docs a module writes): `apps/hub/src/modules/README.md`. Modules: `hub`, `dev`, `founder`, `ops`, `studio`, `brand`, `work`, `spaces`, `design` (0014), `archive` (S-12 / S-13, 0019), `sets` (P-06, 0022), and since 0013 `tools` (D-05 / D-07 / D-08), `qa` (D-09 / D-11 / D-14), `manual` (M-xx), `docs` (D-06 / D-15), `client` (C-xx), `public` (P-xx). Service model (the founder's playbook as typed data): `apps/hub/src/domain/playbook.ts` (D-033); project templates as typed bilingual data: `apps/hub/src/domain/templates/` (D-062, the W-03 source of truth); actions bus: `apps/hub/src/actions/` (D-036; `window.__aluzina.actions`, browsed and run on D-09); plan module: `apps/hub/src/plan/` (D-037). Spaces model (tree, graph builder) and entity index: `apps/hub/src/modules/spaces/{model,entities}.ts` (D-026; registries `roles` / `users` / `services`, D-046). Brand documents as data: `apps/hub/src/data/{schema,seed}/assets.ts` seeded from `docs/brand/<doc>/index.json` (D-044); archived files as data: `seed/archive.ts` from `docs/archive/index.json` (projects, spaces, tags, relations) with `domain/archive.ts` (lifecycle, delivery stages incl. the 2026 folder-template phrases of ar-22, file types) and the library `FileIcon` / `Thumb` / `DocumentViewer` (D-055..D-059, 0019); the files themselves as lazy chunks + patch rows, `data/{archiveFiles,archiveRows}.ts` (D-071, 0022); the client example set `modules/sets` P-06 and the founder's review A-09 (0022); plus the `TagEditor` molecule and the curation permission `archive.curate` (D-065..D-067, 0021); the `Icon` atom and `iconMap.ts` resolve every nav glyph and hub card to an SVG (D-064, 0021); company documents: `seed/company.ts` from `docs/archive/company/index.json` (D-068, 0021); intake pipeline `scripts/archive/{crawl-dropbox.mjs,build-index.mjs,render-previews.py}` (`npm run archive:*`, D-058) and `scripts/archive/index-collection.py` for collections (`npm run archive:collection`, `domain/collections.ts` lazy loader, `seed/collections.ts`, D-083..D-086, 0023). Shared utilities: `apps/hub/src/design/{clipboard,env}.ts`. Shared Work views model and hooks: `apps/hub/src/work/` (nesting, deliverable and child-count helpers since 0020); generated Asana seeds: `apps/hub/src/data/seed/asana/` with `seed/asana.ts` (D-062 / D-063); realtime and presence seam: `apps/hub/src/data/MockProvider.ts`, `apps/hub/src/presence/` (D-023). Parallel module drafts go to `changelog/_pending/<module>.md` and are merged into the next numbered changelog by the integrator (0007 merged four, 0013 merged thirteen, 0021 merged four, 0022 merged five, 0023 merged one).

Page codes (shared vocabulary between specs, docs, screenshots and commits): `HUB-01` hub, `BOS-01` the Business OS app, `BOS-02..06` the other export pages (`pages/BOS.md`), `P-00` the Lovable site (linked), `P-01..P-05` public site inside the OS (services, service, intake, method, portfolio and brochure; changelog 0013, D-035), `P-06` client example set by stateless link (module `sets`, 0022, D-074), `C-01..C-06` client app on PhoneShell (home, project, approvals and revision matrix, messages, payments, brief; 0013), `A-01..A-09` founder admin / approvals (`A-08` leads, `A-03` pipeline on the 15 statuses; 0013; `A-09` archive review of the inferred facts, 0022), `O-01..O-13` operations portal (`O-11` change orders, `O-12` purchasing, `O-13` site reports; 0013), `S-01..S-13` studio (`S-10` service checklist, `S-11` revision matrix; 0013; `S-12` project archive browser and `S-13` project portal on studio / founder / brand, changelog 0019), `G-01..G-09` brand (`G-08` brand documents, 0013; `G-09` campaigns & assets, the Dropbox collections, 0023), `W-01` Work, `W-02` project work (changelog 0008) and `W-03` new project from template (0020), `K-01..K-06` Spaces (K-04 graph in five views since 0013), `M-01..M-08` operations manual (0013), `D-xx` dev / docs / design: `D-02` components, `D-03` specs, `D-04` multiuser, `D-05` plan viewer, `D-06` docs, `D-07` canvas, `D-08` demo simulator, `D-09` actions registry, `D-10` design tokens (`design` module, changelog 0014), `D-11` testing hub, `D-12` brand guidelines and `D-13` textures and effects (0014), `D-14` dev tokens with contrast, `D-15` document view (`/docs/*`) (0013). Next free: A-10, O-14, S-14, G-10, P-07, C-07, M-09, D-16, K-07, W-04. Role map: `knowledge/roles-and-portals.md` (D-013, D-015).

Served renders of the archive: `apps/hub/public/archive/<slug>/{thumbs,pages}/` and of the collections `apps/hub/public/archive/{campaign-2021,studio-assets}/{thumbs,pages,sheets}/` (512 px thumbs, <= 4 pages, contact sheets, 24.68 MiB, D-086, 0023) (committed, <= 640 / 1200 px, the visual memory of a featured project, D-058). Generated at deploy, never committed: `dist/thumbs/<code>.jpg` + `thumbs/manifest.json`, the hub card thumbnails (`scripts/thumbnails.mjs`, D-011).

`qa/` holds the QA matrices (`0001` Work views, `0002` pass 0013, `0003` archive smoke, 0019; `0004` archive at 2560 / 3840 + dark and HUB-01 icons at 3840, 0021; `0005` step 14 pass 3 smoke of S-12 / S-13 / P-06 / A-09 / G-08 / K-05 at 390-3840, 0022; `0006` G-09 collections at 360-3840 light / dark EN / ES, 0023). Planned folders (added when their step starts): `reference/business-os-audit.md` (step 2), `data/`, `rules/`, `ops-manual/`.
