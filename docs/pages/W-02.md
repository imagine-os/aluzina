# W-02 - Project work

- route: `/#/<founder|ops|studio|brand>/work/:projectId` (e.g. `/#/ops/work/prj-laureles`)
- surface: founder | ops | studio | brand
- status: built
- spec: `apps/hub/src/modules/work/specs.ts` (`projectWorkSpec(surface)`)
- model: Fable 5.1

## Purpose

One project in the four Work views: its sections are the list groups, board columns and timeline swimlanes; the header names the project, its client and counts; "All work" goes back to W-01. Opened from the "Open a project…" select on W-01.

## Screenshots

All live, **HOY Wellness Center as ops** since 0020 (the project now carries the imported Asana tree): `docs/screenshots/W-02/en-390.jpg`, `en-1280.jpg`, `en-1920.jpg`, `es-1280.jpg` (List, top of page), `en-1280-nested.jpg` and `en-390-nested.jpg` (the same List scrolled into DISEÑO, where the nesting, the deliverable badges and the child counts read), `en-1280-board.jpg` (Board: 42 top-level cards over the four sections), `en-1280-timeline.jpg` (Timeline: "Nothing is scheduled yet" — the export has no dates).

## Layout (top to bottom)

1. `PageHeader` (W-02): project name, "client · n tasks in s sections · o open", breadcrumb portal / Work / project, "All work" button.
2. `WorkHeader` (project filter hidden).
3. `WorkList` | `WorkBoard` | `WorkTimeline` | `WorkCalendar`.
4. `TaskDetailDrawer`.

## Data

Same as W-01, filtered by `projectId` (tasks and sections); new tasks are created in the project (first section when none is chosen). `prj-hoy` is seeded from the Asana export (`seed/asana.ts`, D-063): 4 sections, 212 tasks, 170 nested, 19 linked to a deliverable, all tagged `asana`.

## Rules

As W-01.

## Logic

As W-01 (including the nesting, deliverable and imported-row behaviour added in 0020), plus: the project id comes from the route; an unknown id renders an `EmptyState` with "All work"; the last view state is remembered per project (`scope = projectId`). `/work/new` is a static segment, so W-03 wins the route ranking over `:projectId`.

## Actions (P-05)

The same 34 `work.*` actions as W-01.

## Components

As W-01 (+ `EmptyState` for the not-found case).

## Real vs mock / placeholder

As W-01; no `Placeholder`.

## Responsive and input check (P-01, P-03)

Smoke at 390 and 1280 (`/ops/work/prj-hoy` and `/ops/work/prj-laureles`): no console errors, no page overflow; inherits the W-01 checks of the organisms at 360-2560. The 212-task HOY tree was the test case for the 390 card layout: a long deliverable name now wraps inside its badge instead of pushing out of the card, and the twisty stays on the first line of a wrapped title.

## Strings (P-13)

As W-01; EN and ES complete.

## Open questions

- Per-project defaults (e.g. the project's lead designer lands on the Board)?
- PROYECTO HOY's identity and zones are unconfirmed (D-063): the project summary carries the flag and nothing here is cleaned up on the founder's behalf.
- Should an imported project get dates from the template's durations, so its Timeline is not empty?
