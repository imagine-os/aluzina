# S-13 - Project portal view

- route: `/#/studio/archive/:projectId` (same page and code also at `/#/founder/archive/:projectId` and `/#/brand/archive/:projectId`)
- surface: studio, founder, brand
- status: built
- spec: `apps/hub/src/modules/archive/specs.ts` (`archivePortalSpec(surface)`)
- model: Opus 5

## Purpose

One archived project as a portal: the example Justin asked for with JOE GALLINA INTERIOR. Everything the studio produced for that project is on one page, in the order it was produced, previewable without leaving the system — "each of the items and deliverables need to be previewable from inside the system", "build a pipeline and organize the documents in order". The page also answers the other half of the ask, "sets of related examples to show specific clients", with a Related block that finds the projects sharing tags with this one.

## Screenshots

`docs/screenshots/S-13/` — `en-390.jpg`, `en-1280.jpg`, `en-1920.jpg`, `es-1280.jpg` (+ `routes.json`), re-captured in the integration pass (changelog 0019) against a local production build (`npm run preview`, version 0.11.0) as `studio` at `/studio/archive/prj-ar-joe-gallina-interior` with the real deep index: 179 files across 9 stages, the cover is the served render of `DISEÑO/03_PLANOS/00 PLANOS ALTOS DE LA TOJA.pdf` (`apps/hub/public/archive/joe-gallina-interior/thumbs/…`), the stage rail counts Concept 9 / Design development 24 / Technical drawings 8 / Quotation 1 / Procurement 109 / Execution 2 / Delivery 2 / Administration 19 / Other 5. The standard captures show the top of the page (hero + stage rail); the files grid, "By folder" and the preview drawer were exercised by the smoke (`docs/qa/0003-archive-smoke.md`: the drawer loads `pages/…-page-01.jpg` at 1200 px, HTTP 200). Redacted files (147, tag `confidencial`, D-059) show the `FileIcon` and the "no preview" sentence by design.
## Layout (top to bottom)

1. `PageHeader`: breadcrumb Studio portal / Archive / project, title, subtitle "N files across M stages of the delivery order".
2. **Hero**: cover `Thumb` (16:9, lg) beside name, client, year, location, project type, `StatusPill`, lifecycle `Badge`, every tag as a `Badge`, and the buttons **Open in Dropbox** (external, `sourceFolderUrl`), **Open in Work** (`/<surface>/work/:projectId`), **Open space** (when a `spaces` row has `aboutId` = the project) and **Add to portfolio set**.
3. **Delivery order**: a horizontal rail built from `DELIVERY_STAGES` in `order`, sub-headed by `pipelineGroup` (Design, Sale, Build, Close — in delivery order, so design comes before sale). Each stage is a >= 44 px button with its glyph, label and file count; a stage with no files reads "no files" in muted text and is still shown, because the empty stages are part of the map.
4. **Files** toolbar: `SearchField`, `Select` stage, `Select` file type, Grid | List `ToggleButton`s, **By stage | By folder** `ToggleButton`s, and a `Placeholder` **Tag file**.
5. **Files**, either one section per stage that holds files (in delivery order, folders inside each stage) or, in "By folder" mode, the studio's own numbered folder tree. Each file is one button holding a `Thumb` (thumbnail or type icon, badge = "N p." for a PDF with a page count, else the extension, caption = the file name).
6. **Preview `Drawer`**: file name as the title, a meta line (type, size, modified, stage `Badge`, folder), the `DocumentViewer`, then Download, Open at source, Copy link, Previous file, Next file and "i of n".
7. **Related**: the client card, the Spaces post about the project, and up to six "Related examples" cards for projects sharing at least two tags.

## Data

Read only. `projects` (the project and the tag-sharing candidates), `assets` (`kind: 'file'`), `relations` (`belongs-to` for this project's files, `for-client` for the client), `clients`, `spaces` (`aboutType: 'projects'`, `aboutId`), `filings` and `posts` (the Spaces post about the project). The only write is `localStorage['aluzina.archive.set']` through "Add to portfolio set".

## Rules

P-01, P-03, P-05, P-06, P-07, P-09, P-13, P-14. D-019, D-026 (relations, not ad hoc fields, carry the client / space / post links), D-047.

## Logic

- **Stage of a file** is `asset.stage`, set at seed time by `stageFor(folderPath, name)`; a file with no stage falls into `other`, so nothing disappears.
- **Two groupings**, in `?group=stage|folder`. By stage: one section per stage that has files, in `DELIVERY_STAGES` order, folders inside sorted with `compareFolderPaths`. By folder: the whole tree in the studio's own numbering, sorted with `folderOrderKey`, each heading a breadcrumb of `folderLabel` segments with the number kept as a small muted prefix ("Diseño > 03 Planos"). Folders that hold no files of their own — an ancestor such as `DISEÑO`, or a numbered folder the studio created and never filled — still render, muted, with "empty folder", so the structure is complete. That is the "documents in order" view.
- **The rail filters and scrolls**: clicking a stage sets `?stage=` and scrolls its section into view; clicking the same stage again clears the filter. Arrow keys (and Home / End) move along the rail; every stage button keeps a normal tab stop, so a keyboard person can also just tab.
- **The preview is URL state** (`?file=<assetId>&page=N`), so a specific file in a specific project is a link. Previous / Next walk the *currently filtered* list, wrapping, and "i of n" says where you are. `page` drives the `DocumentViewer`, which renders the paged image viewer when the asset has `previewUrls`, a PDF object/iframe chain when it has a served `url`, a big image or a `<video>` for those families, and otherwise its own sentence plus the file-type icon — never a blank frame. Escape closes the drawer.
- **Related** is three reverse queries: the client from a `for-client` relation (falling back to a name match while the seed has no relation), the Spaces post through the project's space and its `filings`, and the projects that share two or more tags, ranked by shared-tag count then year, capped at six.
- **Nothing is written** this pass beyond the portfolio set. Tagging a file is declared, `Placeholder`-wrapped and registered behind `assets.manage`.

## Actions (P-05)

| id | label | intent | permission | params |
| --- | --- | --- | --- | --- |
| `archive.openFile` | Preview a file | preview the file {asset} | `projects.read` | asset: id |
| `archive.closeFile` | Close the preview | close the file preview | `projects.read` | — |
| `archive.nextFile` | Next file | show the next file | `projects.read` | — |
| `archive.prevFile` | Previous file | show the previous file | `projects.read` | — |
| `archive.previewPage` | Go to a page | go to page {page} of the open file | `projects.read` | page: number |
| `archive.filterStage` | Filter by stage | show only the files of the stage {stage} | `projects.read` | stage: string |
| `archive.filterFileType` | Filter by file type | show only {type} files | `projects.read` | type: string |
| `archive.searchFiles` | Search the files | find files matching {q} | `projects.read` | q: string |
| `archive.setFilesView` | Switch the file view | show the files as a {view} | `projects.read` | view: enum:grid\|list |
| `archive.setGrouping` | Switch the grouping | group the files {mode} | `projects.read` | mode: enum:stage\|folder |
| `archive.openSource` | Open a file at the source | open the file {asset} in Dropbox | `projects.read` | asset: id |
| `archive.downloadFile` | Download a file | download the file {asset} | `projects.read` | asset: id |
| `archive.copyFileLink` | Copy a file link | copy the link to the file {asset} | `projects.read` | asset: id |
| `archive.openFolderSource` | Open the source folder | open the source folder of this project | `projects.read` | — |
| `archive.tagFile` | Tag a file | tag the file {asset} with {tag} | `assets.manage` | asset: id, tag: string |
| `archive.addToSet` | Add this project to the set | add this project to the portfolio set | `projects.read` | — |

All sixteen register while the page is mounted; `archive.tagFile` registers only for a person with `assets.manage` and answers `'not wired yet: tag editing writes assets.tags via the provider in the next pass'` (D-047). Handlers that take an `asset` accept an id or a file name and fall back to the open file. Every handler returns something readable (the file name, the stage id, the copied URL, or a sentence saying why it could not act).

## Components

`PageHeader`, `Card`, `Thumb`, `FileIcon`, `DocumentViewer`, `Drawer`, `FilterBar`, `SearchField`, `Select`, `ToggleButton`, `Badge`, `StatusPill`, `Button`, `EmptyState`, `Placeholder`, `Toast`.

## Real vs mock / placeholder

Real: the hero, the stage rail with live counts, both groupings, the folder numbering and labels, the file grid and list, the preview drawer with its navigation and copy link, and Related. `Placeholder`: **Tag file**. Mock: the files come from `docs/archive/projects/joe-gallina-interior/index.json`, a stub deep index until the crawler runs, so JOE Gallina Interior shows three files across three stages instead of its real folder tree, and the served renders (`apps/hub/public/archive/<slug>/{thumbs,pages}/`) are not in the build yet — the drawer therefore shows the `DocumentViewer`'s fallback rather than a real page image.

## Responsive and input check (P-01, P-03)

Verified with Playwright against the production build at 360, 390, 768, 1280 and 1920: horizontal overflow 0 px at every width, no console errors. At 1280 and up the stage rail wraps into rows of groups; at 360 / 390 it scrolls horizontally with the focus ring visible. Targets: every stage button is 64 px tall, every file button at least 44 px, the drawer's close and navigation buttons 44 px; the Related links are `Button`s (44 px) rather than inline text links. Keyboard: tab reaches the rail, ArrowRight / ArrowLeft / Home / End move along it, tab continues into the toolbar and then one stop per file, Enter opens the drawer and focus moves into it (focus trap), Escape closes it. Nothing hover-only, nothing drag-only. Light and dark. 2560 / 3840 not captured this pass.

## Strings (P-13)

Namespace `archive.*` (shared with S-12), `apps/hub/src/modules/archive/strings.ts`. EN complete, ES complete (verified at 1280 with `aluzina.lang = es`: the rail reads DISEÑO / VENTA / OBRA / CIERRE with Spanish stage labels).

## Open questions

- The drawer shows "Open at source" twice when the `DocumentViewer` renders its own fallback controls; should the page suppress its action row for those file types, or should the viewer drop its buttons when the host supplies them?
- Should "Related examples" be a saved set (one click to put all six in the portfolio set) rather than six separate cards?
- The client on the archived projects reads "unknown" until the crawler fills `projects.client`; should an archived project without a known client show the year folder instead?
