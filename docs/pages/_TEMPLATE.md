# <CODE> - <Page name>

- route: `/#/<path>`
- surface: hub | business-os | website | customer | staff | docs | manual | dev
- status: built | stub
- spec: `apps/<app>/src/modules/<module>/specs.ts`
- model: <who built it>

## Purpose

One paragraph: who uses it and for what.

## Screenshots

`docs/screenshots/<CODE>/<lang>-<width>.jpg` — list the captures (EN and ES, matrix widths verified).

## Layout (top to bottom)

1. ...

## Data

Tables read / written (or "none").

## Rules

Business rule ids implemented (or "none").

## Logic

Behaviour that is not obvious from the layout: state, persistence, keyboard shortcuts.

## Actions (P-05)

| id | label | intent | permission | params |
| --- | --- | --- | --- | --- |

## Components

Library components used (each has a `.meta.ts`).

## Real vs mock / placeholder

What works for real, what is a `Placeholder`, what is mock data.

## Responsive and input check (P-01, P-03)

Widths verified (`spec.checkedAt`), focus order, targets, hover-only audit, notes for 2560 / 3840.

## Strings (P-13)

Namespace, EN complete? ES complete?

## Open questions
