# Kanban

One card per line. Steps refer to `build-plan.md`. Model per card in parentheses.

## Backlog

- Asana CSV importer behind K-06 (`docs/source/asana/2026-09-21/*.csv`): sections -> phases, parent-by-name resolution, `Task ID` as externalId, notes `KEY: value` forms -> fields, Zone / trade facets; seed PROYECTO HOY as a real project once the founder confirms which zones are HOY's (Opus 5)
- Project template entity + "create project from template" (copy the ALUZINA WORKFLOW tree in either language, choose zones / phases) so 2023 residue cannot leak into new projects (Fable 5.1 model, Opus 5 UI)
- Vendor job fields on purchases: `profitAluzina`, `aluzinaResponsibility` enum, payment schedule rows; O-12 columns and C-05 (Opus 5)
- Document generators from the Asana workflow: cotización Excel from spec items, RFQ packet per trade, process-progress slide for the client presentation, acta de entrega y correcciones (Fable 5.1 design, Opus 5)
- Awaiting founder: is PROYECTO HOY = HOY Wellness Center, and are ZONE 1..11 HOY's spaces or 2023 template residue? Sarai or Saray? (Justin)
- Next intake: the standardized per-project Dropbox tree and the Canva document types (drive-scraping channel) (Sonnet 5)
- QA defect (docs/qa/0002, changelog 0016): Markdown table renderer has no minimum column width or horizontal-scroll container, so any wide table (confirmed on `/docs/decisions.md`, D-06 / D-15) wraps every header and body cell one letter per line at 390 and 1280, unreadable rather than merely cramped; wrap the rendered `<table>` in a scrollable `.table-wrap` or set `white-space: nowrap` cells (Opus 5)
- QA defect (docs/qa/0002, changelog 0016): D-14 `/dev/tokens` "Copy variable" button label wraps mid-word at 390 ("variabl"/"e"); shorten the label at narrow widths or set `white-space: nowrap` (Sonnet 5)
- Live screenshots of the 33 portal pages still missing since changelog 0007 (A-01/A-02/A-04..A-07, O-02..O-10, S-02..S-09, G-02..G-07) at 390 / 1280 EN (+ 1280 ES per dashboard); not in this pass's code list (Sonnet 5)
- Spaces follow-ups: Slack export import script behind K-06 (channels -> spaces, messages -> posts, threads -> comments, pins, mentions -> relations), space move / reorder UI (`spaces.admin`), tag registry editor, per-space visibility enforcement (`role:<id>`, `private`), post versions, file posts with storage (Supabase), 2560 / 3840 captures, K-05 catalog tab for `assets` (Opus 5)
- Graph views follow-ups (changelog 0013, D-053): label collision pass (hide / stagger labels by zoom), frame the focus neighbourhood instead of the bounds in 3D, re-check the 140-node cap once a Slack import lands, `Tooltip` atom (hover **and** focus) replacing the in-organism card (Opus 5); `pageCode` as data on deliverables / tools and `K-0x` routes in `scripts/thumbnails.mjs` (Sonnet 5)
- Component library requests from pass 0013 (P-07, owners Opus 5): `DocumentViewer` organism taking an `Asset` row (`<object>` -> `<iframe>` -> sentence, download, page fallback; G-08, P-05), `Stepper` / `FormStep` (P-03, C-01, M-01), `Accordion` (C-02, S-10), `ProgressBar` / `Meter` (C-01, C-02, O-12, O-13, S-10, KPIs), `Checklist` (M-xx, S-08, S-10), `MessageBubble` / `Thread` (C-04), `Swatch` (D-14, G-08), `ParamForm` (D-09; move `paramSchema()` / `toolJson()` to `src/actions/` with the WebMCP generator), `DeviceFrame` + `ZoomStage` + `DependencyLane` (D-05 / D-07 / D-08), `Markdown` heading ids + `linkResolver` (D-06), `SpaceTree` -> `Tree` naming, `Slider`
- Pass 0013 shared-code backlog (Fable 5.1): file-storage seam on the `DataProvider` (`put(file) -> url`: O-13 photos, G-08 replace document, floor plans in the intake, O-08 documents); `useGovernance()` / `canEnter(project, status)` in `src/domain` shared by S-10 / A-03 / O-11 + `GovernanceRule.pageCode[]`; Work views migrate `phase` -> `pipelineStatus`, then drop `phase` and `founder.moveProject` (D-048); `leads` row-level policy for anonymous visitors + rate limiting before `/start` goes on a public domain; `documents.clientVisible`, `own.brief.write`, unread badge on the client bottom nav, client sign-off on change orders (`clientApprovedAt`), `purchases.quoteId`; `leads.read` view for brand / marketing or drop the permission; `postMessage` seam (`aluzina.setLang / setTheme / navigate`) for D-08; `docs:index` build step; D-14 dev tokens vs D-10 design tokens: one page or a clear split; G-01 documents row from `assets`; `deliverables.playbookAssetId`
- Pass 0013 QA follow-ups (Sonnet 5): shadow elevations onto the 0014 `shadow` tokens (Toast, Modal, Drawer, HubHeader, DevTools), `specCompleteness` `checkedAt` threshold to the full seven widths, `qa.runAction` may not run itself, D-05 swimlane collapse-by-lane / compact mode, D-07 fit second press, S-11 / D-11 wide tables from 768 to ~1500 px, Spanish review of `domain/playbook.ts` with the founder (English fallbacks show inside Spanish manual pages), 2560 / 3840 reads at ten feet for tools and client
- Archive intake (roadmap phase, D-030): social and Google Drive ingestion into Spaces posts + repo files (website done, changelog 0011); depends on Justin's instructions in the `drive-scraping` / `social-scraping` channels (Claude + GitHub connected there) and on Drive / social access (Fable 5.1 plan, Sonnet 5 passes)
- Awaiting Justin: connect the Lovable project to GitHub under the imagine-os org (steps in prompt 0007), then post the repo name in #website-scraping (Fable 5.1)
- Platform roadmap (D-030): Comms (channels, DM, presence; then calls, video, email, SMS, WhatsApp via integrations), CRM (leads, pipeline; extends A-06 + `clients`), Documents and e-sign (extends O-08), Drafting / CAD viewer + render pipeline, Research assistant over the actions registry, Finance (Stripe), Public website builder (replaces Lovable), Marketing production + scheduler, Integrations layer (Slack, Asana, Google) (Fable 5.1 architecture, Opus 5 modules)
- Marketing portal M-xx for the `marketing` role (today lands on `/brand/spaces`); code prefix to settle with the ops manual `M-xx` (Opus 5)
- D-020 data layer (rest): entities `clients`, annotations (P-08, can reuse `comments`), `partnerships`, `products`, `goals`, `followUps`, `comparisonGroups`, `scheduleItems`, `measurements`, `images` + file storage; fields `payments.supplierId`, `materials.imageUrl`, `references.imageUrl`, `revisions.imageUrl`, `brandAssets.thumbnailUrl` (`comments`, `tasks.own.write` done in 0008) (Fable 5.1)
- Step 11 Work follow-ups: pointer drag to move board cards and shift timeline bars (bonus over the select / drawer paths), section create / rename / reorder UI, `views` table replacing `aluzina.views.<userId>` (D-025), 1920 / 2560 / 3840 and dark captures of W-01 / W-02, an "Open in Work" link from A-07 / S-01 / G-01 task cards (Opus 5)
- Step 11 realtime: Supabase Realtime + Presence adapters behind `subscribe` / `usePresence()` (D-023), `version` column + merge UI replacing last-write-wins (D-024), conflict log rows for D-04 (Fable 5.1)
- D-020 alert generation: one rule raising / resolving `alerts` from payments, quotes, deliveries and competition deadlines (`leadDays` before `dueDate`), surfaced per role (Fable 5.1)
- D-020 seeds: two or three more `plan` / `spec` documents across Casa Laureles, Noam, Café Provenza; competition rows when the 2027 list arrives (Sonnet 5)
- D-020 product: shared "request" flow (presentations, revisions, image sets) from the studio / ops / founder portals into the brand queue; report templates for O-10; code-split the bundle per module (590 kB chunk warning) (Opus 5)
- Portal polish pass: 1920 / 2560 / 3840 captures of every portal page, dark-theme pass, `checkedAt` to the full matrix (Sonnet 5)
- Step 3: module split, PageSpecs, component metas, page docs, screenshots per width; `qa:responsive` + strings-coverage check (Opus 5, Sonnet 5)
- Media budget: convert `apps/business-os/assets/world/*.png` (93 MB) to webp / downscale once the audit says which are kept (Sonnet 5)
- Step 5 (rest): client proposal view of the full stack; a `public` shell in `app/shells.tsx` if a second bare-with-chrome module appears (Opus 5)
- Step 6: Spanish fill pass over every strings table (Sonnet 5)
- Step 7 (rest): `feedback` table + FeedbackButton (P-08; D-11 "File a bug" becomes real), WebMCP tool generation from `window.__aluzina.actions.declared` (the bus and D-09 exist, changelog 0013) (Fable 5.1)
- Step 8 (rest): realtime plan doc `docs/reference/realtime-plan.md`, offline queue (presence and the realtime seam landed in 0008) (Fable 5.1, Opus 5)
- Step 9c: alert pattern across portals (Miguel's "before urgent" + Angelica's deadlines from `alerts`, lead-time rule, surfaced per role) (Fable 5.1)
- Dev tools next: `viewAs` control in the user menu for founder / dev (Opus 5)
- Data: `version` column + conflict UI, `CompanyOsProvider` stub (reference only), Supabase adapter behind `DataProvider` (Fable 5.1)
- Component library follow-ups: `FeedbackButton` (P-08) (Sonnet 5)
- Knowledge: fill `docs/knowledge/competitions.md` (the 20 competitions and submission dates) and surnames / languages in `team.md` when the founder shares them; confirm "Hoy" = HOY Wellness Center; fill Sporti (sector, city, project), client contacts, real deliverable durations and templates (Fable 5.1)
- Hub thumbnails: dark-theme variant (`thumbs/<code>-dark.jpg`) and 2x tiles for 4K once the card grid is checked at 2560 / 3840 on the live site (Sonnet 5)
- Manual export defects -> Angélica: stale `#98876D` hex under the 877 C swatch, PRODUCTOS / ESPACIOS / ARTE labels still gold, section rules and descriptor slashes are zero-width fills (invisible); re-export, then replace `apps/hub/public/brand/MANUAL-DE-MARCA-ALUZINA.pdf` and the dated copy in `docs/source/brand-kit/` (D-052) (Angélica; Sonnet 5 for the swap)
- Descriptor: which lockup is primary, "INTERIORISMO / ILUMINACIÓN" (under the logo) or "UNIVERSO DE DISEÑO" (DESCRIPTOR section and footer)? Both ship as `BrandMark` variants, `universo` default (D-052) (Justin / Angélica)
- Slider atom for the D-13 shader intensity and future range controls (label, min / max / step, value text, 44 px thumb, keyboard and voice); replaces the five-step Select (Opus 5)
- Licensed DIN Round Pro woff2 drop-in to `apps/hub/public/fonts/` with the licence note in its README (D-040) (Justin / Angélica)
- Founder pipeline compact tiles (`.founder-stats--compact`) clip the eyebrow "DOCUMENTATION" at 1280+; min-width bump in `founder.css` (Sonnet 5)
- Brand rules the manual lacks: clear space, minimum size, misuse, photography direction, tone of voice; house rules are labelled on D-12 until then (Angélica)
- Awaiting Justin: default language en vs es for the hub and the OS (D-004)
- Awaiting Justin: does the public site join the OS as the P-xx module now live at `/#/services` (P-01..P-05, changelog 0013), or stay Lovable-hosted (P-00) with the OS behind it?
- Awaiting founder: which brand era is current (portfolio *Universo de Diseño* vs brochure *Interiorismo · Iluminación*); playbook services vs brochure trades (Mobiliario, Iluminación; the Arte pillar); product line = Honey Valley? (`knowledge/brand.md#differences-vs-the-playbook`, changelog 0013)
- Awaiting founder: the brochure's 17 brochure-only client names as `projects` rows, and whether "Apartamento Parma Noham Ebresum" is `cl-noam` (D-045)
- Awaiting Justin: pick the next pass from prompt 0006: step 2 export audit (Fable 5.1) or the media budget webp / downscale pass (Sonnet 5)

## Doing

- Social intake (Instagram): public capture of aluzina.espacios blocked by login wall / 429, and the one permitted retry against @aluzinaa also blocked (429; the error-shell evidence was withdrawn, D-038); intake now waits on Justin's handle confirmation or Instagram data export in #social-scraping (Fable 5.1; changelog 0012 + correction, D-032, D-038)
- Step 2: audit the export against P-01..P-15: en/es toggle, responsive matrix, inputs, placeholders, actions registry; write `docs/reference/business-os-audit.md` (Fable 5.1, Sonnet 5) — next

## Done

- Asana intake (changelog 0017, D-054): six CSV exports saved de-identified under `docs/source/asana/2026-09-21/`, `knowledge/asana-conventions.md` written (structure, the two language templates, HOY vs both templates, vendor-job form, Dropbox / Drive / Excel / Canva / WhatsApp, OS data-model and document-generation implications), knowledge README / tools-in-use / clients / deliverables extended (Fable 5.1 analysis, Sonnet 5 files)
- Pass 0013 QA follow-up (changelog 0016, docs/qa/0002): live screenshots of all 37 pass-0013 codes (95 files, `docs/screenshots/<CODE>/`) captured against a local `npm run preview` build of the deployed commit — the live GitHub Pages URL couldn't be reached directly because the sandbox's auto-mode classifier denied the Chromium TLS-trust flag the external capture needed, and that denial was honored rather than worked around; content is identical since Pages serves the same build. 2 defects found and filed to Backlog (D-14 button-label wrap at 390, D-15/D-06 Markdown-table letter-wrap at 390 and 1280). Spanish fill pass: 0 missing `es` keys across every `apps/hub/src/modules/*/strings.ts` and `i18n/core.ts` (already complete going into this pass), 3 of 224 `domain/playbook.ts` `T()`/`L()` calls filled (`LEAD_CHANNELS`: instagram, whatsapp, networking); `npx tsc --noEmit` clean. `spec.checkedAt` gained 3840 on D-05 and K-04 where this pass's captures added a width. Build green (Sonnet 5)
- Silver edition of the brand manual (changelog 0015, D-050..D-052): `metalDefault: 'silver'` (877 C `#C0C0C0`, smooth ramp white -> `#4D4D4D`, per-metal `--gradient-metal*` and `--metal-stop-*`), neutral greys in both themes (light accent and warning nudged to pass contrast), iridescent wordmark as the primary logo in shells, hub header and hero, `BrandMark` descriptor variants (`universo` / `interiorismo`), `.surface-ink` black band, D-12 rewritten for silver with the PDF download button (`design.downloadManual`, `public/brand/`), four discs, defects section; both manual editions under `docs/source/brand-kit/`; build green, console clean at 390 / 1280 light + dark, PDF 200 under `base: './'`, captures HUB-01 / D-10 / D-12 / D-13 / O-01 (Fable 5.1)
- Pass 0013 integration (changelog 0013, D-044..D-049, D-053): twelve drafts merged, renumbered against the design-system session (D-14 / D-15, prompt 0012, version 0.10.0), shared fixes in the library (`Button` `download` + wrapping labels, `Select` / `Textarea` required marker, `DataTable` keyboard-scrollable region, `.dshell__portal` ellipsis, presence bar under 480 px, print rule, `design/clipboard.ts`, `design/env.ts`, `core.portal.*`), 21 thumbnail targets + SwiftShader, rebase onto 0014, `npm ci` + build green, smoke over 113 routes x 2 widths (226 checks, 3 overflows fixed), deploy (Fable 5.1)
- Pass 0013 foundation (D-033..D-037): `knowledge/service-playbook.md`, `domain/playbook.ts`, entities `leads / engagements / revisionItems / changeOrders / purchases / siteReports / messages`, permissions, nav groups, actions bus + `window.__aluzina.actions`, `docs/plan/plan.json` + `@docs`, route-derived hub cards (Fable 5.1)
- Pass 0013 data: brand documents as relational rows (prompt 0013, D-044..D-046): `assets` (2 documents + 56 pages), 13 portfolio projects, 6 clients, Spaces > Portfolio, 126 relations, `services` registry, `depicts` kind; G-08 / P-05 read from data; 37 entities, SEED_VERSION 6 (Fable 5.1)
- Pass 0013 public site P-01..P-05 (D-035, D-047): services ladder, service pages by slug, the intake writing `leads` rows with the routing suggestion, the method page, portfolio and brochure page; EN + ES complete (Opus 5)
- Pass 0013 client app C-01..C-06 on PhoneShell: journey and progress, project detail, approvals + revision matrix with the G-06 gate, messages with read receipts, payments, strategic brief (Opus 5)
- Pass 0013 operations manual M-01..M-08: every word from `domain/playbook.ts`, print / PDF, live `routeService()`, governance page deriving the enforcing pages from the manifest (Opus 5)
- Pass 0013 in-app docs D-06 / D-15: the repo's Markdown through `@docs` (lazy chunks), tree, full-text search, in-app relative links, `plan.json` as a table, every document has a URL (Opus 5)
- Pass 0013 builder tools D-05 plan viewer (Kanban / table / dependency swimlane), D-07 canvas (zoom that reflows), D-08 demo simulator (phone + desktop frames) (Opus 5)
- Pass 0013 QA tools D-09 actions registry (declared vs live, run, WebMCP JSON), D-14 tokens with contrast, D-11 testing hub (seven-width matrix, specCompleteness) (Opus 5)
- Pass 0013 CRM and execution control (roadmap R2 first pages, D-048): A-08 leads with convert-to-project, A-03 pipeline on the 15 statuses with the G-06 / G-12 gate in the write, O-11 change orders (G-14), O-12 purchasing control, O-13 site reports (Opus 5)
- Pass 0013 studio S-10 service checklist (engagements.checks, phase advance, procurement gate) and S-11 revision matrix (decide, CSV export) (Opus 5)
- Pass 0013 graph views K-04 (prompt 0012, D-053): five views (3D objects with three.js lazy chunk, lanes, radial, objects map, force 2D fallback) with system imagery, `GraphViews` organism (Opus 5)
- Pass 0013 brand documents (prompt 0011, D-049): PDFs served from `apps/hub/public/brand/`, 56 page renders + `index.json` as visual memory in `docs/brand/`, `knowledge/brand.md` part B, G-08 brand documents page with related projects / services, hub card G-08 (Opus 5)
- Brand design system (changelog 0014, D-039..D-043): the ALUZINA manual as tokens (`tokens.ts` -> `tokens.css`, metal gold / silver group, gradients, textures, DIN Round Pro + Rubik), `BrandMark` and `Shimmer` (WebGL2) atoms, module `design` on its own `design` surface (D-12 brand guidelines with gold / silver preview, D-10 live tokens with contrast in both themes, D-13 textures and effects), `design.read` for every role, hub hero band + D-12 card (Builder and dev tools) and the Tokens card lit by D-10, restyle of both shells and the library components (34 CSS files) with a dark-mode audit, `--theme=dark` screenshots; 78 routes, 688 action entries; build green, console clean at 390 / 1280 / 1920 light + dark, captures for HUB-01 / D-10 / D-12 / D-13 / O-01 / A-01 in the repo; prototype untouched (Fable 5.1, Opus 5, Sonnet 5)
- Archive intake: aluzinaa.com (18 pages, text + HTML shell + 22 screenshots 390-3840) and direccion.aluzinaa.com (full static source + 5 screenshots) captured into docs/source/aluzinaa-archive; knowledge/public-sites.md; Lovable GitHub-sync guidance posted (changelog 0011, D-031) (Fable 5.1, Sonnet 5, Opus 5)
- QA fixes (changelog 0010): five 44px-target / 16px-font defects from the Work views QA matrix (docs/qa/0001) — view-tab and timeline-bar min-heights, row-select checkbox hit label made explicit, shell role badge font at 1920, W-02 breadcrumb "Work" link min-width; re-verified at 390/1280/1920, build green (Sonnet 5)
- Step 12: Spaces, the Hub's relational organizer replacing the Slack sidebar (changelog 0009, D-026..D-030): entities `spaces / posts / filings / relations / tags / clients / deliverables / tools` (SEED_VERSION 4: 42 spaces, 30 posts, 80 filings, 53 relations, 25 deliverables, 6 clients, 9 tools), role `marketing`, `Markdown` / `SpaceTree` / `PostCard` / `RelationGraph` in the library, module K-01..K-06 on five surfaces (75 routes), hub card, knowledge docs taxonomy / deliverables / clients / tools, smoke green as ops and founder at 390 / 1280 / 1920 light + dark, keyboard tree + open post, two-tab filing in 8 ms (Fable 5.1)
- Step 10: Asana-style Work views (changelog 0008, D-021..D-025): `sections` / `comments` / `activity`, six organisms + `PresenceBar` with metas and examples, MockProvider BroadcastChannel realtime with rows in the message, `basedOn` conflicts + toast, `PresenceProvider` + shell bar, Work module W-01 / W-02 on four portals with role defaults, D-04 multiuser page, O-02 / O-03 "Open in Work", `tasks.own.write`, smoke green at 360-2560 for all four views, keyboard-only drawer run, two-tab update in 15-20 ms (Fable 5.1)
- Step 9c: integration (changelog 0007, D-018..D-020): Placeholder wrapper mode + tooltip clamp, StatTile numbers, Timeline bar targets, router future flags, `suppliers.read`, `tasks.startDate`, thumbnails image-wait bound; tsc + build green; smoke over 36 routes at 390 / 1280 clean; `_pending` drafts merged (Fable 5.1)
- Step 9b: Founder portal A-01..A-07 (approvals, pipeline, quotes and proposals, products and partnerships, clients, team) (Opus 5)
- Step 9b: Operations portal O-01..O-10 (schedule, tasks, suppliers, quotes and comparisons, deliveries, payments, documents, alerts, reports) (Opus 5)
- Step 9b: Studio portal S-01..S-09 (projects and proposals, references, palettes, plans, schedules, packs, consistency check, measurements) (Opus 5)
- Step 9b: Brand portal G-01..G-07 (competitions 2027, presentations, identity and assets, images, revisions, asset library) (Opus 5)
- Step 9a: portal foundation (changelog 0006, D-014..D-017): `src/auth/` (roles, permissions, demo users, SessionProvider, RequireRole, useCan, `?as=` contract), registry + manifest with shell / permission / nav, DesktopShell + PhoneShell, DataProvider + MockProvider + 18 seeded entities + `useTable / useRow / useData`, 24 new components with metas and live examples, `PageSpec` extensions + `specCompleteness`, D-02 `/#/dev/components`, D-03 `/#/dev/specs`, hub Portals section + RoleSwitcher + `hub.enterAs` / `hub.switchRole`, portal stubs A-01 / O-01 / S-01 / G-01, thumbnails for the new codes, `src/modules/README.md` (Fable 5.1)
- Hub thumbnails generated at deploy time (D-011): `scripts/thumbnails.mjs` + `npm run thumbs` in the Pages workflow, `SurfaceCard` image slot with bilingual fallback tile, `thumbs/manifest.json`; 9 thumbs in 30 s locally (Fable 5.1)
- Knowledge base `docs/knowledge/` with change tracking (D-012): README convention, `team.md` (Alejandra Guerra, Miguel, Sarai, Angelica), `competitions.md`, `roles-and-portals.md` (D-013); prompt 0002, changelog 0005 (Fable 5.1)
- Step 0: monorepo layout (`apps/hub`, `apps/business-os` README, `docs/`, root workspaces) (Fable 5.1)
- Step 0: HUB-01 hub: EN/ES toggle, light/dark, dev mode, SpecChip + actions panel (Ctrl+.), Placeholder atom, tokens -> tokens.css with `--scale` bands, `window.__aluzina` manifest (Fable 5.1)
- Step 0: Pages workflow (`.github/workflows/pages.yml`), build green locally (Fable 5.1)
- Step 0: docs tree: README, principles, brief, build plan, decisions D-001..D-006, kanban, prompt 0001, changelog 0001, page template + HUB-01, surfaces (Fable 5.1)
- Step 0: first Pages deploy green (run 35542778592), live at https://imagine-os.github.io/aluzina/ (HTTP 200); live screenshots 390 / 1280 / 3840 EN + 390 ES in `docs/screenshots/HUB-01/` (Fable 5.1)
- Step 1: prototype verified live at https://imagine-os.github.io/aluzina/business-os/ (run 35543723076; zero external requests; EN/ES toggle works); screenshots BOS-01 (390 / 1280 / 3840 EN, 1280 ES) and BOS-02 (1280) in `docs/screenshots/` (Fable 5.1)
- Step 1: Claude Design export ingested at `apps/business-os/` (uploads dropped, D-009), runtime + fonts vendored (D-008), URL-safe entry points (D-010), copied into `dist/business-os/` by the root build, hub card BOS-01 live + Prototype pages BOS-02..06 (Fable 5.1)
- Justin's second Drive link delivered the correct 288 MB zip (D-006 resolved)
