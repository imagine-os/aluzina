# Decisions

One row per decision, append-only. Reversals add a new row and mark the old one **superseded**. `Source` is the prompt number.

| Id | Date | Decision | Why | Source | Status |
| --- | --- | --- | --- | --- | --- |
| D-001 | 2026-09-20 | **Monorepo** with npm workspaces `apps/*`; the hub (`apps/hub`) is the root of the GitHub Pages site and builds into repo-root `dist/`. | Justin asked for the repo to be the monorepo for Aluzina; the hub is the house-rule entry point (P-12) and must own `/`. | 0001 | accepted |
| D-002 | 2026-09-20 | **GitHub Pages via the Actions workflow** (Petrock's `pages.yml`: checkout@v5, setup-node@v5 node 22, `npm ci`, `npm run build`, configure-pages@v5 `enablement: true`, upload-pages-artifact@v3 `dist`, deploy-pages@v4); `react-router-dom` HashRouter; Vite `base: './'`. | Same as Hoy / Petrock: no 404 fallback needed, relative assets, deploy on push to `main`. Pages source must be "GitHub Actions" in Settings. | 0001 | accepted |
| D-003 | 2026-09-20 | The **Claude Design export lives at `apps/business-os/`** and mounts at **`/business-os/`** under the hub (`dist/business-os/`), keeping its own package and build. | Keeps the prototype intact for the audit (step 2) while the hub stays the root; the root build appends its build when it lands. | 0001 | accepted |
| D-004 | 2026-09-20 | **English primary** string tables `{ en, es? }` with `useT()`; Spanish falls back to English; language persisted as `aluzina.lang`; toggle on every surface from the start. | House rule (P-13) and Justin: "english/spanish toggle is important". Open question: Aluzina and its customers are Spanish-first, so the **default** language may flip to `es`; the key shape does not change either way. | 0001 | accepted (default language open) |
| D-005 | 2026-09-20 | **Petrock platform principles P-01..P-15 adopted as binding** (`docs/platform-principles.md`, adapted for Aluzina). | They are the current expression of the workspace admin rules; adopting them verbatim keeps every imagine-os project on one bar. | 0001 | accepted |
| D-006 | 2026-09-20 | The **288 MB Slack export cannot be fetched by the agent** (50 MB cap on Slack file pulls). Transfer path pending from Justin: a zip under 50 MB without `node_modules` / `dist` / `.next`, a Drive / Dropbox link, or Justin drops the folder into the repo himself. Scaffold proceeds without it. | Unblocks steps 0 and the docs; step 1 waits on the transfer. | 0001 | accepted (waiting) |
| D-007 | 2026-09-20 | **Keep the Claude Design export as a static bundle** in `apps/business-os/` (no port to React / Vite yet); the root build copies it into `dist/business-os/` with `scripts/copy-static.mjs`. | The export has no build step (dc-runtime + Babel in the browser); serving it unchanged gives Justin and the owner the exact prototype today and keeps the step 2 audit honest. Porting is step 3. | 0001 | accepted |
| D-008 | 2026-09-20 | **Vendor the runtime locally**: React 18.3.1, ReactDOM 18.3.1, @babel/standalone 7.29.0 (SRI-verified) in `apps/business-os/vendor/`, Google Fonts CSS + 18 woff2 in `vendor/fonts/`; `support.js` patched (one `[aluzina]` hunk: the three CDN constants) and each page's `<helmet>` font `<link>` pointed at `vendor/fonts/` with the CDN `preconnect`s removed (the browser fetches helmet stylesheets while parsing, before the runtime runs). | No external single point of failure, works offline / kiosk, no third-party requests from the prototype. The design files stay untouched; the patch is documented in `docs/reference/business-os-export.md`. | 0001 | accepted |
| D-009 | 2026-09-20 | **Drop `uploads/`** (140 MB of byte-identical duplicates of `assets/`, grep-verified unreferenced) and keep its two documents (`plan.md`, SELAV diagnostic PDF) under `docs/source/claude-design-export/`. | Halves the repo, loses nothing the pages load; the documents are business knowledge, not runtime assets. | 0001 | accepted |
| D-010 | 2026-09-20 | **URL-safe entry points alongside the original filenames**: `index.html`, `home.html`, `cyber-bridge.html`, `cyber-bridge-deck.html`, `image-generation-plan.html`, `lod-ladder.html` forward (JS `location.replace` + noscript meta refresh, query and hash preserved) to the space-containing `.dc.html` files, which remain canonical. | The runtime derives the component name from the filename (`SELF`, sibling fetch), so renaming would break self-embeds; forwarders give the hub clean links without touching the export. | 0001 | accepted |
| D-011 | 2026-09-20 | **Hub thumbnails are generated at deploy time from the live build, never committed**: `scripts/thumbnails.mjs` (`npm run thumbs`) runs in the Pages workflow after `npm run build`, serves `dist/` locally, screenshots every hub-linked surface with Playwright Chromium (1280 x 800 -> 640 x 400 JPEG) into `dist/thumbs/<code>.jpg` + `manifest.json`; `SurfaceCard` reads `./thumbs/<code>.jpg?v=<buildId>` and falls back to a bilingual tile. `npm run build` does not depend on it. | Justin: "image thumbnails that stays up to date, so i know what is what for each item". Committed images drift from the pages and bloat the repo; generating from the deployed build guarantees they match what is live and keeps local builds fast. | 0002 | accepted |
| D-012 | 2026-09-20 | **`docs/knowledge/` is the canonical domain knowledge base** with explicit change tracking: every entry carries `status: current \| superseded \| draft`, `since`, `source`, `supersedes`; superseded entries are kept with a pointer, never deleted; each file ends with a dated `## Change log`. Claude's channel memory holds pointers only; the repo wins on conflict. Rendered in-app at `/#/docs` later. | Justin: "make sure there is memory in the system as we add more info and context"; admin rule: a knowledge base with change tracking so outdated rules are never mistaken for current ones. | 0002 | accepted |
| D-013 | 2026-09-20 | **Role -> portal map** (`docs/knowledge/roles-and-portals.md`): Founder `A-xx` admin / approvals, Administration and Operations `O-xx`, Interior Design `S-xx` studio, Graphic Design and Communication `G-xx` brand / competitions, Client `C-xx`, developers `D-xx`; each with the permission strings it implies; all planned (build plan step 9). Roles are strings checked through `can()`; demo users per role in the hub role switcher, never the real team members' data. | The founder's roster names the people who "will have their own views/portals"; reserving the code namespace and permissions now lets step 3 assign the prototype screens to owners and step 4 build the role switcher against real names. | 0002 | accepted (portals planned) |
| D-014 | 2026-09-21 | **Portals are modules of the single hub app**: one Vite build (`apps/hub`), HashRouter paths `/founder`, `/ops`, `/studio`, `/brand` (client `/client` planned, dev `/dev/*`); `src/modules/<name>/index.ts` exports `{ routes, strings }` and the registry globs them (`src/modules/README.md`). Every portal page renders inside `DesktopShell` (sidebar from the surface's routes grouped by `navGroup`, top bar with role badge, EN/ES, theme, dev mode, back to hub, user menu). | One deploy, one design system, one session; four module workers can build in parallel without touching shared files (Petrock pattern). Separate apps would multiply builds, tokens and auth. | 0003 | accepted |
| D-015 | 2026-09-21 | **Role model**: roles are strings (`founder`, `ops`, `studio`, `brand`, `client`, `dev`), permissions are strings (`src/auth/permissions.ts`, e.g. `projects.approve`, `quotes.compare`, `design.develop`, `brand.manage`, `dev.tools`; founder `*`); one demo user per role (`demoUsers.ts`, real first names from the roster, invented client and dev, no real contact data); `SessionProvider` (`aluzina.session`: user, viewAs, devMode) with `switchUser` / `viewAs` / `can()`; `RequireRole` guards every route by permission and renders a real bilingual "switch role" page; `?as=<role>` before the hash selects a demo user on first load. Identity is mocked, guards are real (P-12). | Pages never compare roles, so real auth (Supabase) later only swaps the identity source; the hub can open every portal as any role today; thumbnails and QA can address a role by URL. | 0003 | accepted |
| D-016 | 2026-09-21 | **Mock DataProvider first, Supabase behind the same interface later**: `src/data/provider.ts` (`list / get / create / update / remove / subscribe / reset`, every row `id, created_at, updated_at`), `MockProvider` (in-memory + localStorage `aluzina.data`, `SEED_VERSION`), seeds globbed from `src/data/seed/*.ts` (18 entities for a Medellín interior design and lighting studio, COP amounts, unknown facts `null`), `useTable / useRow / useData` hooks re-rendering from subscribe events. Company-OS stays reference only (P-15). | Portals need realistic data now; the seam keeps writes by id and event-driven lists so realtime (P-14) and Supabase are adapters, not rewrites. | 0003 | accepted |
| D-017 | 2026-09-21 | **Component library tiers and "no hand-rolled UI in pages"**: `src/components/<tier>/<Name>/<Name>.tsx + .css + .meta.ts + .example.tsx` (atom, molecule, organism, template); `src/design/library.ts` globs metas and examples; `/#/dev/components` (D-02) renders every component from its meta with a live example. Pages use only library components; a missing component is added to the library first (or wrapped in `Placeholder` and requested). | P-07: tables, design system and component library are managed in the product; four parallel workers must share one vocabulary and one look from day one. | 0003 | accepted |
| D-018 | 2026-09-21 | **The four role portals ship as modules**: Founder `A-01..A-07` (`/founder/*`), Operations `O-01..O-10` (`/ops/*`), Studio `S-01..S-09` (`/studio/*`), Brand `G-01..G-07` (`/brand/*`), each built by one worker inside its module folder (D-014 contract), all routes `built` with declared actions (148 in total). The ops range is extended to `O-10` because "formatting and laying out reports" is its own roster line; page ranges per module are now A 01-07, O 01-10, S 01-09, G 01-07 (next free codes A-08, O-11, S-10, G-08). Cross-module hand-offs are single writes on shared rows: studio check passed -> `projects.approval = awaiting-founder`; founder approve -> `approved` + alerts resolved; founder comment / request changes -> a task for the lead designer; ops select quote -> winner `selected`, group `rejected`. | One deploy, four points of view, no shared files edited by the modules; the ranges record what exists so screenshots, docs and voice vocabulary line up. | 0003 | accepted |
| D-019 | 2026-09-21 | **`Placeholder` becomes a non-interactive wrapper around controls**: when its child is a library `Button`, a `<button>` or an `<a>` (or `bare` is set) it renders a `<span>` that decorates the child (tooltip on hover / focus / focus-within, toast on activation, dev badge, `data-placeholder`) instead of wrapping it in a second `<button>`; plain content still gets the real button. The tooltip is `display: none` until shown and anchored to the trigger's end edge, so it never widens the page. | All four modules reported `button > button` (React `validateDOMNesting`, duplicate tab stop) and 40-90 px of horizontal scroll from the hidden tooltip; one fix in the atom instead of four workarounds (P-03, P-07, P-09). | 0003 | accepted |
| D-020 | 2026-09-21 | **Deferred shared requests from the module pass, with owners** (kanban backlog): data layer (Fable 5.1): entities `clients`, `comments` / annotations (P-08), `partnerships`, `products`, `goals`, `followUps`, `comparisonGroups`, `scheduleItems`, `measurements`, `images` + file storage; fields `payments.supplierId`, `materials.imageUrl`, `references.imageUrl`, `revisions.imageUrl`, `brandAssets.thumbnailUrl`; alert generation from due dates (one rule for Miguel's chases and Angélica's deadlines); `tasks.own.write` for the studio. Seeds (Sonnet 5): more `plan` / `spec` documents; competitions when the 2027 list arrives. Product (Opus 5): shared "request" flow into the brand queue, report templates (document templates are first-class), code-split per module. Done now instead: `suppliers.read` for studio and ops, `tasks.startDate`. | The pass stays on its scope (four portals on the existing entities); the requests are real and recorded once, not lost in four drafts. | 0003 | accepted (backlog) |
| D-021 | 2026-09-21 | **Asana-style List / Board / Timeline / Calendar are shared organisms, never per-portal forks**: `WorkHeader`, `WorkList`, `WorkBoard`, `WorkTimeline`, `WorkCalendar`, `TaskDetailDrawer` in `src/components/organism/` over one model (`src/work/model.ts`: filters, sort, grouping, due windows, cycle check) and one module (`src/modules/work/`) mounted on every portal surface with a role default; the older `Kanban` / `Timeline` / `Calendar` / `DataTable` stay for the pages that use them. Moves happen through a "Move to…" select and prev / next buttons, dates through the drawer; pointer drag is a bonus, never the only path (P-03). | The team knows Asana's views; one implementation keeps the vocabulary, keyboard model and look identical in four portals and gives voice / WebMCP one action set (`work.*`). Forks would drift on the first fix. | 0004 | accepted |
| D-022 | 2026-09-21 | **Project-management entities**: `sections` (project or studio-wide, ordered), `comments` (`entity`, `entityId`, `authorId`, `body`) and `activity` (`entity`, `entityId`, `actorId`, `field`, `from`, `to`, `at`, written by the provider on every update, one row per changed field, capped at 500); `tasks` gain `sectionId`, `description`, `createdById`, `tags`, `subtasks`, `completedAt`, `order`; rows gain optional `updated_by`. `SEED_VERSION` 3. | Sections are the unit Asana users group, column and swimlane by; comments and activity give the drawer its thread and "who changed what" line, and the D-04 page its log; `createdById` makes `tasks.own.write` checkable. Comments also close the D-020 request. | 0004 | accepted |
| D-023 | 2026-09-21 | **BroadcastChannel is the mock realtime and presence seam**: `MockProvider` posts every write on `BroadcastChannel('aluzina-data')` carrying the changed rows (fallback: `storage` event), receiving tabs upsert and fire the same `subscribe` event; `PresenceProvider` heartbeats `{ tabId, userId, route, at }` on `BroadcastChannel('aluzina-presence')` every 5 s, expires after 15 s (fallback: `aluzina.presence` in localStorage). Supabase Realtime and Presence replace the channels behind the same `subscribe` / `usePresence()`; pages never touch the channels. | P-14 asks for realtime and presence without designing against them; two tabs of the same browser are the cheapest real multiuser test and exercise the exact code path (event -> `useTable` re-render) a server push will use. | 0004 | accepted |
| D-024 | 2026-09-21 | **Interim conflict rule: last-write-wins with a notice.** Writes carry `basedOn` (the row's `updated_at` the edit started from); when the stored row is newer the provider still applies the write and fires `onConflict({ entity, id, by, at })`; the app toasts "Updated by <name> just now; your change was applied over theirs" (`updated_by`). Field-level merge and versioned rows come with the Supabase adapter. | Small team, short edits: blocking or silently dropping a write is worse than telling the person who they just overrode. The seam (`basedOn`, `onConflict`) is what a `version` column will strengthen. | 0004 | accepted (interim) |
| D-025 | 2026-09-21 | **Saved views live per user in localStorage** (`aluzina.views.<userId>`: named views + last state per scope) until a `views` table exists; role defaults are code (founder list by project + approvals, ops timeline, studio board, brand list filtered to her). | Views are personal and cheap; a table is only worth it when views are shared or synced across devices, which needs auth first. | 0004 | accepted (interim) |
| D-026 | 2026-09-21 | **Spaces are a graph model**: `spaces` nest without limit (`parentId`), `posts` are filed many-to-many through `filings` (one row shown in every space it is filed in; unfiling from one leaves the others), `relations` are typed (`references, applies-to, part-of, replaces, depends-on, belongs-to, produced-by, for-client, owned-by`) and any-to-any (`fromType / toType` are entity names or the `roles` / `users` registries), backlinks are the reverse query. `spaces.aboutType / aboutId` says what a role, client, deliverable, tool or project space is about. The Markdown body renders through a small safe renderer of our own (React elements, no HTML); the graph (K-04) uses our own deterministic force layout (no dependency). Module `src/modules/spaces/` mounts K-01..K-06 on founder, ops, studio, brand and dev; permissions `spaces.read` (all), `spaces.write` (founder, ops, brand, marketing, dev), `spaces.admin` (founder, dev). | Justin's two complaints about Slack are exactly one level of nesting and no relations; a tree plus many-to-many filings plus typed edges removes both without inventing a CMS. Own renderer and layout keep the bundle and the attack surface small. | 0005 | accepted |
| D-027 | 2026-09-21 | **The Slack sidebar is seeded as the initial space tree and the Hub is the organizing mechanism from now on.** Sections -> areas, channels -> spaces (slug = channel name), plus a Projects area and an Archive; 30 example posts filed 80 times in several spaces each, 53 relations; `SEED_VERSION` 4. Slack stays for chat until the Comms module ships; K-06 is where a Slack export lands (read-only mapping + contract today). Justin's later channels (drive / website / social scraping) join the tree as topics with draft intake posts. | The structure Justin built is the best available map of what the Hub needs; seeding it makes the switch immediate and keeps his vocabulary. | 0005 | accepted |
| D-028 | 2026-09-21 | **Slack role channels map to roles and `marketing` is a new role**: owner -> `founder`, administrative-assistant -> `ops`, interior-design-jr -> `studio`, marketing-strategist -> `marketing` (new; `marketing.plan / content / channels`, `spaces.*`, `tasks.own.write`; lands on `/brand/spaces` inside the brand surface until a marketing portal exists; demo user Valentina, invented), customer-portal -> `client`, developer -> `dev`. `ROLES` has seven entries; `ROLE_META.marketing` shares the brand surface. | The sidebar names a marketing strategist the roster did not; a role without a portal is still a point of view for permissions and for the "for your role" space. Minimal change: no new surface, no new module. | 0005 | accepted |
| D-029 | 2026-09-21 | **Deliverables, clients and tools are data entities rendered in K-05, with knowledge docs as the change-tracked narrative**: `deliverables` (25 types with phase, owner role, template kind, status `defined / template-ready / automated`), `clients` (6: Hoy, Sporti past; Restrepo, Noam, Ruta N current; Provenza prospect; unknowns `null`), `tools` (9 with status and `replacedByModule`). `docs/knowledge/{deliverables,clients,tools-in-use}.md` record sources, unknowns and changes; the rows are the truth the pages read. | Justin said the lists are only started; entities can be edited in the product and related from posts, while the docs keep provenance so an outdated fact is never mistaken for a current one (D-012). | 0005 | accepted |
| D-030 | 2026-09-21 | **Platform roadmap "own everything, integrate where we must"**: phases with dependencies in `docs/build-plan.md` (Comms, CRM, Documents and e-sign, Drafting / CAD viewer and render pipeline, Research assistant, Finance, Public website builder, Marketing production and scheduler, Integrations layer, Archive intake). Build ourselves: chat / channels / presence, CRM, documents and approvals, scheduler, website, research assistant over the actions registry, import / export. Integrate rather than rebuild: telephony / video / SMS / WhatsApp transport, payments (Stripe), e-sign legal validity, AI image generation and upscaling (Lovart / Magnific class), CAD file parsing. | Justin: "not be reliant on other software at all, only their own ... maybe with some integrations". Naming which parts are transport / legal / model commodities keeps the ambition and the delivery honest. | 0005 | accepted |
| D-031 | 2026-09-21 | **aluzinaa.com stays Lovable-hosted and gets its own Lovable-created GitHub repo under imagine-os; the Hub links to it and archives it, it does not absorb the code.** | Lovable sync only works with a repo Lovable creates, on one branch, and publishing stays a manual Lovable step; direccion.aluzinaa.com is plain static HTML, archived in full at `docs/source/aluzinaa-archive` and re-hostable from the repo. | 0007 | proposed (awaiting Justin; also resolves the open kanban question about whether the public site joins the OS as a P-xx module) |
| D-032 | 2026-09-21 | **Social intake is sourced from owner-provided data first** (Instagram's own data export uploaded to Slack, or a Graph API token for the account), with public scraping kept only as a fallback for evidence and cross-checks. Handle to ingest is confirmed by the founder before any capture is treated as Aluzina's. | Anonymous access to instagram.com is rate-limited to a login wall from the agent egress (HTTP 429 `is_from_rle`) and all public mirrors are challenged or blocked; `aluzina.espacios` resolves to Instagram's 404 shell while aluzinaa.com links `@aluzinaa`. | 0008 | accepted |
| D-033 | 2026-09-21 | **The founder's Service Delivery Playbook v1.0 is the canonical service model** (`docs/knowledge/service-playbook.md`, typed in `apps/hub/src/domain/playbook.ts`): five services `01 / 02 / 03 / E / 04` with every phase / stage and checklist item, the 10-step client journey, lead intake (7 channels, lead record, commercial data, 10 qualification questions, routing heuristic `routeService()`), the service ladder, 14 governance rules `G-01..G-14` (nine mandatory + commercial rule, method principle, approval gate, execution principle, change control), roles, 11 operational assets, 10 KPIs. `projects.pipelineStatus` and `leads.status` adopt the **15-status architecture** (`lead-new … follow-up`, kebab-case ids, groups `lead / sale / design / build / close`); `projects.phase` (seven phases) and `projects.approval` are kept for the Work views and the approval chain until they migrate. Spanish texts are the integrator's translations, to be reviewed by the founder. | Justin: "save this to memory ... follow all the instructions to build out the software further". The playbook is the studio's own method; typing it once gives the public site, the CRM, the client app, the studio checklists, the manual and the voice vocabulary one source (P-11, D-012). | 0009 | accepted |
| D-034 | 2026-09-21 | **Playbook entities and their owners** (`src/data/schema/services.ts`, `SEED_VERSION` 5, 36 entities): `leads` (owner ops / founder; `leads.manage`, `leads.read`), `engagements` (one service on one project: `currentPhaseId`, `checks` keyed `${phaseId}:${itemIndex}`, `brief`; studio / ops / founder, `engagements.write / read`), `revisionItems` (the single revision matrix per project, statuses `approved / approved-with-adjustments / revision`; studio, founder, ops write, client `own.revisions.write`), `changeOrders` (`requested / approved / rejected / executed`; ops, founder, `changeOrders.manage`), `purchases` (`quoted / approved / paid / ordered / received / installed`; ops, `purchases.manage`), `siteReports` (ops, studio, `siteReports.write`), `messages` (project thread, studio and client, `messages.write`). `projects` gain `serviceCode` and `pipelineStatus`. Seeds: 8 leads, 6 engagements, 6 revision items, 2 change orders, 6 purchases, 3 site reports, 10 messages. | Each playbook control (revision matrix, change order, purchasing control, site control, official channel) is a table with a status vocabulary, not a field on `projects`, so every portal reads the same rows and the governance rules can be checked in data (G-03..G-08). | 0009 | accepted |
| D-035 | 2026-09-21 | **The public module `P-xx` ships inside the OS as the services pages + intake flow** (`/services`, `/services/:slug`, `/start` on the `public` surface, bare shell, no permission) rendering `SERVICES` and writing `leads` rows (`source: 'public-intake'`, `status: 'lead-new'`, `suggestedService` from `routeService()`). **aluzinaa.com stays linked as `P-00`** until Justin decides whether the Lovable site is replaced (kanban card stays; consistent with D-031, proposed: the Lovable site keeps its own repo and the Hub links to it). The **payment step** of service activation (01 phase 1 "once payment is confirmed") is a `Placeholder` until Stripe (D-030). | The playbook's commercial process starts at the website ("every inquiry must enter a single traceable pipeline"); building the intake in the OS removes the copy-paste from the Lovable form without pre-empting the website decision. | 0009 | accepted (P-00 decision open) |
| D-036 | 2026-09-21 | **Actions bus is the WebMCP seam** (`src/actions/bus.ts`): pages call `useRegisterAction(id, handler)` for every action they declare while mounted; `runAction(id, params)` returns `{ ok, result?, error? }` (`error: 'not-live'` when nothing is mounted for it); `listLiveActions()`, `subscribeLiveActions()`, `declaredActions(routes)` flatten the manifest. Published as `window.__aluzina.actions = { run, list, declared }` next to `routes` (P-05, P-06). D-09 renders declared vs live and runs actions; WebMCP tools and the voice controller call `run` with the same ids. | The registry was declared-only since 0001; a bus that pages register on while mounted keeps one code path for UI, voice and agents (P-06) and costs each page one hook per action. | 0009 | accepted |
| D-037 | 2026-09-21 | **`docs/plan/plan.json` is the machine-readable development plan** (`{ version, updatedAt, source, tasks: [{ id, title, step, status: done \| doing \| next \| backlog, model, codes[], dependsOn[], owner?, changelog? }] }`), imported by the app through the `@docs` Vite alias + tsconfig `paths` (`src/plan/index.ts`: `PLAN`, `planOrder`, `planBlockers`, `planDepth`, `planCounts`) and rendered by the PM viewer D-05 as kanban / list / timeline with dependencies. **`build-plan.md` and `kanban.md` stay the human view and must agree with it**: a kanban move edits both in the same turn; tasks are bound by dependencies, never dates. | The admin rule asks for a PM viewer as the first deliverable; the docs are already the plan, so the JSON is a projection the app can read rather than a second source of truth. | 0009 | accepted |
| D-038 | 2026-09-21 | **D-032 stands with its evidence corrected**: the Instagram error shell (`httpErrorPage` / `PolarisErrorRoot`) is served to all anonymous datacenter clients and is not diagnostic; the handle question rests on the website link (aluzinaa.com -> `@aluzinaa`) and the search index (no `instagram.com/aluzina.espacios` indexed) only. | The capture worker's control fetch of the certainly-live `/aluzinaa/` at 04:36 UTC returned the identical shell (HTTP 200); the one permitted browser attempt on `@aluzinaa` got 429 + login redirect, so nothing was captured for either handle. Decisions are append-only, so D-032's "Why" is left as written and corrected here. | 0008 | accepted |
| D-039 | 2026-09-21 | **Brand manual is canonical; metal is one switchable token group.** The palette from MANUAL DE MARCA ALUZINA (gold 875 C `#98876D`, periwinkle 270 C, aqua 3245 C, lime 379 C, ink `#231F20`) replaces the amber cues taken from aluzinaa.com; gold is `tokens.metal.gold`, `tokens.metal.silver` is defined (candidate Pantone 877 C, provisional), `metalDefault: 'gold'`; `gen-tokens.mjs` emits `--metal-*` from the default and every metal as `:root[data-metal]`; runtime preview through `<html data-metal>` (localStorage `aluzina.metal`, set by D-12). Shipping silver is one line + `npm run tokens`, re-deriving `--color-accent*`, `--color-hairline`, `--color-metal-text` in the same change. | Justin: "we will likely change the gold to silver"; a one-line switch keeps the whole system consistent instead of a redesign later. | 0010 | accepted (silver Pantone provisional) |
| D-040 | 2026-09-21 | **Typography: DIN Round Pro with Rubik fallback, one family.** `fonts.css` declares the five faces at `/fonts/DINRoundPro-*.woff2`; licensed files are dropped into `apps/hub/public/fonts/` (never committed without a web licence, see its README); `--font-display` = `--font-sans`; hierarchy by weight and tracking (`.eyebrow`, `--tracking-caps`); Playfair Display and Roboto removed. | The manual names one family; Rubik is the closest free rounded DIN and keeps the site readable until the licence lands. | 0010 | accepted |
| D-041 | 2026-09-21 | **The design system lives in the product as module `design` on its own `design` surface**: D-12 brand guidelines (`/design`), D-10 tokens (`/design/tokens`, the hub's Tokens card), D-13 textures and effects (`/design/effects`); permission `design.read` for every role (founder `*`); hub card D-12 first in Builder and dev tools; `docs/design/brand-system.md` is the written brief and `docs/knowledge/brand.md` the facts. | P-07 (design system managed in the product) and Justin's ask for brand guidelines in the hub; the pages read the same `tokens.ts` the app ships with, so they cannot drift like a PDF. | 0010 | accepted |
| D-042 | 2026-09-21 | **Shader and textures are decorative progressive enhancement.** `Shimmer` (raw WebGL2, no dependency) and the `.texture-*` masks never carry information: `aria-hidden`, ink text on metal / iridescent surfaces, one static frame under `prefers-reduced-motion`, CSS gradient fallback without WebGL2, renders only in the viewport, one finish per screen. | Justin asked for realistic textures and shader iridescence; P-03 forbids meaning that depends on motion or hover, and every page must read correctly with the shader off. | 0010 | accepted |
| D-043 | 2026-09-21 | **Dark mode rules are inferred and provisional.** The manual shows only white plus one gold band; dark uses a warm near-black base (`#121110`, not blue-black), gold outlines lifted for contrast (`--color-metal-text` `#D9C9A6`), iridescent wordmark on dark and metal wordmark on light, pastel tints as deep versions of the same hue, ink text on metal / iridescent surfaces in both themes; both themes are checked on every page (light + dark captures). | Justin: "Make sure light and dark mode are great"; the source gives no dark rule, so ours is a house reading until the founder confirms. | 0010 | accepted (draft until Angélica confirms) |
| D-044 | 2026-09-21 | **`assets` is the entity for the files the studio publishes or keeps as brand memory, and a document's pages are rows too** (`src/data/schema/assets.ts`: `kind: document \| page \| image \| logo \| texture`, `url` for what the app serves, `repoPath` for what only the repo holds, `parentId` page -> document (also a `part-of` relation), `palette` / `fonts` / `textExcerpt` read from the file, `status` + `supersedesId` for versions). **`docs/brand/<doc>/index.json` is the seed source** (imported through `@docs` like `plan.json`, D-037): re-rendering a PDF updates the data, nothing about a document is typed twice; `docs/brand/README.md` stays the human view. | Justin asked for the portfolio and brochure to be "saved by project and relational in the proper way in the database". A page is the unit that shows a project or argues for a service, so it must be relatable; a versioned row is what file storage will later fill. | 0013 | accepted |
| D-045 | 2026-09-21 | **Portfolio projects are `projects` rows** (`prj-pf-<slug>`, `pipelineStatus: closed`, `phase: delivered`, `approval: client-approved`), with `clients` rows only when a page names one, a project space under the `sp-portfolio` area and one note per project. Placeholders are explicit and documented in `knowledge/brand.md` (`startDate` `2024-01-01`, `location` `Ubicación no publicada`, `client` `unknown`, `budgetCop: 0`); the brochure's twenty names stay a note until the founder confirms them. | "By project" means the Work views, relations and graph work unchanged on the studio's past work; a closed project is still a project. Unknown facts are named as unknown, never guessed. | 0013 | accepted |
| D-046 | 2026-09-21 | **`services` is a relation-target registry** (`modules/spaces/entities.ts`, id = playbook service code, next to `roles` / `users`) so posts, pages and anything else can `applies-to` a service without a `services` table; **relation kind `depicts`** (an asset page -> the project it shows) joins `RelationKind`, `references` stays for mentions in text. | The playbook is typed data (D-033), not rows; the registry pattern already carried roles and users. The Post page's kind selector enumerates `RELATION_KINDS`, so the new kind appears everywhere at once. | 0013 | accepted |
| D-047 | 2026-09-21 | **A declared action that is not wired registers and fails loudly** (house rule): a `Placeholder` control's action is still registered while mounted and its handler returns / throws `not wired yet: <where it will live>`, so `runAction` answers `{ ok: false, error }` instead of `not-live`, and D-09, voice and WebMCP see the same truth the tooltip shows. **The public site addresses services by slug, never by code**: `/services/:slug` and `public.openService` take `creative-digital-consultation` etc.; `?service=` accepts a code or a slug so WebMCP can pass either. | `not-live` reads as "wrong page"; a readable refusal is the honest answer everywhere the button is not. The codes `01 / 02 / 03 / E / 04` are the studio's shorthand and mean nothing to a client; slugs also survive a renumbering. | 0009 | accepted |
| D-048 | 2026-09-21 | **`projects.phase` and `projects.pipelineStatus` coexist until the Work views migrate.** `pipelineStatus` (15 statuses) is the pipeline of record from 0013 on (A-03, A-08, S-10 and the gates read and write it); `phase` (7) stays the Work views' grouping vocabulary, written only from the A-03 drawer's Select labelled "legacy" and by `founder.moveProject`; nothing derives one from the other (the mapping is not one-to-one). **Governance is enforced in the write, not only in the control**: G-06 / G-12 (A-03, S-10) and G-14 (O-11) re-read the stored row and refuse with a readable reason; the disabled control is the explanation, the write is the rule. A lead is **converted, never promoted**: at `contracted` it becomes a `projects` + `engagements` pair and keeps `projectId`. | The same functions are reachable from the actions bus where no button state exists; a rule that lives only in a disabled button is no rule. Migrating the Work views mid-pass would have touched every portal. | 0009 | accepted |
| D-049 | 2026-09-21 | **Client-facing documents are served assets until file storage exists**: the PDFs ship in `apps/hub/public/brand/` (Vite `publicDir`, `./brand/<slug>.pdf`), their `assets` rows carry size and page count, replacing one is a commit that edits the file and the index together. **Page renders are visual memory, not served**: `docs/brand/<doc>/page-NN.jpg` (<= 1400 px, q80, ~12 MB budget per intake) stay in the repo with a contact sheet, a prose index and `index.json`; `assets.page.url` is `null`. An embedded document always degrades to a sentence with a download link, never a blank rectangle. The marketing material does **not** supersede the playbook (D-033): disagreements are recorded in `knowledge/brand.md#differences-vs-the-playbook` and put to the founder. | No entity drift between "the file" and "what the page says about the file"; 8.6 MB of renders would double the site for a fallback nobody asked for yet (the founder decides); a session can see the brand without opening a PDF (P-11). | 0011 | accepted |
| D-050 | 2026-09-21 | **Silver is the canonical metal (877 C `#C0C0C0`), the neutral base follows, and the iridescent wordmark is the primary logo.** `tokens.metalDefault = 'silver'`; `metal.silver = { base #C0C0C0, highlight #FFFFFF, shade #4D4D4D }` with the manual's smooth ramp `#FFFFFF -> #E0E0E0 37% -> #999999 69% -> #4D4D4D` as `gradient` and five resampled `stops` for the SVG defs; every metal is emitted with its own `--gradient-metal*` and `--metal-stop-*` on `:root[data-metal]`. The warm gold-tinted greys are re-derived neutral in both themes (light bg `#FAFAFA` / text `#111111` / hairline `#C0C0C0` / metal-text `#5C5C5C`; dark bg `#0E0E0E` / text `#F2F2F2` / hairline `#5A5A5A` / metal-text `#C0C0C0`); status colours and tints unchanged; light `accent` `#8A8A8A` and light `warning` `#956710` adjusted to pass 3:1 / 4.5:1. `BrandMark kind="wordmark"` defaults to `finish="iridescent"` in both themes (shells, HubHeader, hub hero); the metal wordmark stays available. Gold is kept as the previous edition, preview only (`<html data-metal="gold">`). | Justin: "Here is the updated brand manual with the silver instead of gold ... make sure that the design system is updated according to this". The silver manual's vector data is `#C0C0C0` throughout with RGB / CMYK agreeing, its only metallic shading is the smooth ramp, its LOGO section paints the wordmark iridescent on white and its band is black: a silver primary on warm greys would clash, so the neutrals move with it (as D-039 foresaw). | 0014 | accepted (supersedes the gold values of D-039; D-039's switch mechanism stands) |
| D-051 | 2026-09-21 | **The brand manual PDF is served from the hub.** `apps/hub/public/brand/MANUAL-DE-MARCA-ALUZINA.pdf` is the current edition, linked from D-12 with a primary library `Button` (`href="./brand/…"` + `download`) and the action `design.downloadManual` (`design.read`); the file is replaced in place when a new edition lands. Source editions stay under `docs/source/brand-kit/` as `MANUAL-DE-MARCA-ALUZINA-<edition>-<date>.pdf` with their extraction notes (`extraction.md` gold, `extraction-silver.md` silver). | Justin: "Make sure we can download this p. D. F from the hub." A static file under Vite's `public/` needs no route or permission, works under `base: './'` on Pages, and the relative link survives the hash router; keeping dated copies in `docs/source/` preserves the history the knowledge base points at. | 0014 | accepted |
| D-052 | 2026-09-21 | **Manual export defects are reported, not reproduced.** The silver PDF has zero-area fills where the six section rules, both DESCRIPTOR slashes and the footer slash should be strokes (they render invisible), a stale `#98876D` hex printed under the 877 C swatch, and PRODUCTOS / ESPACIOS / ARTE labels still gold. The design system follows the evident intent (1 pt silver rules and slashes, `#C0C0C0`, silver labels) and D-12 lists the defects for Angélica under "Manual export defects to fix". The manual also carries two descriptors (INTERIORISMO / ILUMINACIÓN under the logo, UNIVERSO DE DISEÑO in the DESCRIPTOR section and the footer): both ship as `BrandMark` variants (`universo` default) until Justin and Angélica name the primary. | Copying an export bug into the product would freeze it as brand; naming it in the product (where the brand team reads it) gets it fixed at the source. The descriptor question is a decision the founder owns, not ours. | 0014 | accepted (primary descriptor open) |
| D-053 | 2026-09-21 | **The graph views adopt the imagine-os graph gallery, and three.js is accepted for the 3D view.** K-04 offers five views over the same graph data: 3D objects (default), Lanes skill tree, Radial tree, Objects map and the dependency-free Force 2D fallback, rebuilt on the techniques of `imagine-os/graph-gallery` (no code copied: the repo has no licence file). `three` 0.186.0 (MIT, 139 kB gzip) is a runtime dependency loaded only by the lazy 3D chunk; a device without WebGL or with reduced motion gets the 2D graph automatically. Nodes carry system imagery (initials, deploy-time page thumbnails, space / post / catalog glyphs), never bare dots. **Partially supersedes D-026's "no dependency" clause** for the 3D view only. Numbered D-053 because D-038 (reserved for it) was taken on `main` by the D-032 correction and D-050..D-052 are reserved by the design-system session. | Justin asked for these views by name and for pictures from the actual system; the 3D view needs a WebGL engine and writing one costs far more than a pinned, lazy MIT dependency, while every 2D path stays free of it. | 0012 | accepted |
